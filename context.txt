============================================================
LG PILATES BOOKING SYSTEM — CONTEXT FILE
Last updated: 15 Jun 2026 (Session 45)
============================================================

ABOUT THIS FILE
---------------
This is a working context file, NOT a diary. It exists to give Claude
fast, durable context at the start of any session.

Rules to keep it useful:
- Capture WHAT IS TRUE NOW, not the narrative of how we got here.
- Session summaries belong in commit messages and chat history, not here.
- Before adding a "Session N: ..." note, ask: is this a durable gotcha
  that would help a future session, or is it just history? If history,
  leave it out.
- If a fact is already covered in a structured section above, don't
  re-state it as a key decision below.

============================================================
1. PROJECT OVERVIEW
============================================================

A complete Pilates class booking system for LG Pilates (Louise George).
Classes run in Baildon and Guiseley.
Single HTML file (index.html) deployed to GitHub Pages.
Backend: Supabase (Postgres database).
Mark is the non-developer owner. Claude is the primary technical collaborator.

LIVE URL:    https://mjones2420-netizen.github.io/lg-pilates-booking/
GITHUB:      https://github.com/mjones2420-netizen/lg-pilates-booking
DEMOS:       https://mjones2420-netizen.github.io/lg-pilates-booking/demos/
             (11 standalone interactive HTML demo files; several outdated
             since per-class priority + pro-rata + 4-step flow — see TO DO)

index.html is uploaded manually by Mark at the start of each session.
The GitHub connector is no longer used.

============================================================
2. SUPABASE PROJECTS
============================================================

PRODUCTION
----------
URL:        https://mrlooyixnlxzcfmvnqme.supabase.co
Project ID: mrlooyixnlxzcfmvnqme
Anon Key:   eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6Im1ybG9veWl4bmx4emNmbXZucW1lIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NzI5MTM2MDAsImV4cCI6MjA4ODQ4OTYwMH0.k1LbLenXS29NosaayGnQh3SR2G1xXVDBICeH7w7bD_c

TEST
----
URL:        https://ngzfhamjuviwfwuncrjo.supabase.co
Project ID: ngzfhamjuviwfwuncrjo
Anon Key:   eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6Im5nemZoYW1qdXZpd2Z3dW5jcmpvIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NzY1OTM1ODksImV4cCI6MjA5MjE2OTU4OX0.okVYL1VLSDd3I7nD5VPuUCOZmY_G7mzC4o3Y3lx16Pg

Schema is a byte-for-byte mirror of production. Verify with:
    npm run schema-check
Both should return fingerprint dad311c8a152bc0dbec1cb3fa0c584d5, 248 objects.
NOTE: Session 42 added pending_bookings table to both DBs, and Session 43 added
stripe_payment_intent_id + stripe_checkout_session_id columns to bookings on both
DBs — schema fingerprint will have changed twice since the value above. Run
npm run schema-check to get the new value and update here.

Test admin user:
  Email:    admin@lg-pilates-test.local
  Password: stored in tests-playwright/.env.test as TEST_ADMIN_PASSWORD
  IMPORTANT: Test admin users MUST be created via the Supabase dashboard
  "Add user" button. Raw SQL inserts into auth.users do NOT populate
  auth.identities, which Supabase Auth requires for password login.

============================================================
3. ENV SWITCH (?env=test)
============================================================

index.html supports a URL query parameter ?env=test that switches the
Supabase client (data and auth) from production to the test project.

- No query param  → production project, no banner
- ?env=test       → test project, red banner injected at DOMContentLoaded

Code lives at the top of the script section in index.html (around line 800).
Banner: fixed-top red div, id="test-mode-banner", class="on" — this is
the assertion target Playwright uses in every CB spec's beforeEach as a
second line of defence against tests hitting production.

Production behaviour is byte-identical to before the switch existed when
the URL has no ?env=test parameter.

============================================================
4. DATABASE SCHEMA
============================================================

TABLES
------
classes                  — permanent class slots (id, name, level, day, time, end_time, venue, loc)
blocks                   — each 6-week block run (id, class_id, start_date, end_date, weeks, dates[], price, cap, booked, visible, status)
bookings                 — client bookings (id, class_id, block_id, customer_id, status, amount_due, refund_amount, created_at,
                           refund_status, stripe_payment_intent_id [nullable], stripe_checkout_session_id [nullable])
                           NOTE: stripe_payment_intent_id + stripe_checkout_session_id added Session 43
                           (ALTER TABLE, both nullable text, no migration file yet — see TO DO).
                           NULL in bank transfer mode. Populated by stripe-webhook (PM-4) on
                           checkout.session.completed.
customers                — client records (id, first_name, last_name, email, phone, customer_type, priority, created_at)
                           NOTE: customers.priority column is retained in DB but no longer used by the app.
                           Per-class manual priority is now managed via customer_class_priority table.
parq                     — health forms (id, booking_id, customer_id, age, emergency_name, emergency_relationship, emergency_phone, q1_heart...q12_other_reasons, yes_details, additional_notes, print_name, sign_date)
                           NOTE: sign_date is DATE type. Front-end stores ISO (YYYY-MM-DD) in a data-iso
                           attribute on the display element and sends that direct to the DB.
                           Admin PAR-Q view uses formatDateDisplay() to render "18 Apr 2026".
settings                 — key/value store for bank details, admin email, and payment config
                           Keys: bank_name, bank_sort_code, bank_account_no, admin_email,
                           payment_mode ('bank_transfer' | 'stripe'), stripe_publishable_key
                           NOTE: Stripe secret key and webhook secret are NEVER stored here —
                           they live in Supabase Edge Function environment variables only.
waitlist                 — placeholder for future waitlist feature (id, class_id, customer_id, created_at)
                           RLS enabled. Empty — not yet connected to booking flow.
pending_bookings         — temporary holding table for Stripe bookings in progress
                           Columns: id (UUID PK), class_id, block_id, first_name, last_name,
                           email, phone, customer_type, amount_pence, parq_data (JSONB),
                           created_at, expires_at (2 hours after creation)
                           RLS: anon INSERT allowed; authenticated SELECT/DELETE only.
                           Rows are inserted by the stripe-checkout Edge Function (service role)
                           and deleted by the stripe-webhook Edge Function after booking confirmed.
                           Abandoned rows expire after 2 hours (manual cleanup needed until a
                           scheduled function is added).
cancellations            — audit trail of all client removals from blocks
                           Columns: id, customer_id, class_id, block_id, first_name, last_name, email,
                           class_name, venue, block_start_date, block_end_date, sessions_attended,
                           sessions_remaining, price_per_session, refund_amount, refunded (BOOLEAN),
                           refunded_at (TIMESTAMPTZ), cancelled_at (TIMESTAMPTZ)
                           NOTE: first_name/last_name/email/class_name/venue are denormalised so records
                           survive even if the customer or class is later deleted.
customer_class_priority  — per-class manual priority grants
                           Columns: id (BIGSERIAL), customer_id (BIGINT FK → customers),
                           class_id (BIGINT FK → classes), granted_at (TIMESTAMPTZ)
                           UNIQUE constraint on (customer_id, class_id)
                           ON DELETE CASCADE on both FKs
                           A row present = that customer has manual priority for that class.
                           This table replaces the global customers.priority boolean.

NOTE: parq table uses emergency_name, emergency_relationship, emergency_phone (NOT emergency_contact — dropped)
NOTE: parq.booking_id FK is ON DELETE CASCADE — PAR-Qs auto-delete when their booking is deleted.
NOTE: bookings.block_id FK is ON DELETE CASCADE — prevents orphan bookings.
NOTE: App-level deletion code still clears child rows explicitly as belt-and-braces safety net.
NOTE: When bulk-deleting all client data, deletion order must be:
parq → cancellations → bookings → customer_class_priority → customers

CHECK CONSTRAINTS
-----------------
bookings:
  - bookings_status_check: status IN ('reserved','confirmed','cancelled','refund-pending','refunded','waitlist')

blocks:
  - blocks_status_check:    status IN ('upcoming','active','completed','cancelled')
  - blocks_price_nonneg:    price >= 0
  - blocks_cap_positive:    cap >= 1
  - blocks_weeks_positive:  weeks >= 1

customers:
  - customers_type_check:      customer_type IN ('new','returning','vip') OR customer_type IS NULL
  - customers_first_name_len:  char_length(first_name) <= 100
  - customers_last_name_len:   char_length(last_name)  <= 100
  - customers_email_len:       char_length(email)      <= 200
  - customers_phone_len:       char_length(phone)      <= 40

parq:
  - parq_yes_details_len:      char_length(yes_details)            <= 4000
  - parq_additional_notes_len: char_length(additional_notes)       <= 4000
  - parq_emergency_name_len:   char_length(emergency_name)         <= 100
  - parq_emergency_rel_len:    char_length(emergency_relationship) <= 50
  - parq_emergency_phone_len:  char_length(emergency_phone)        <= 40
  - parq_print_name_len:       char_length(print_name)             <= 100

IMPORTANT: customer_type must be lowercase 'new' or 'returning' — the
customers_type_check constraint rejects capitalised variants.

NOT NULL CONSTRAINTS
--------------------
customers:    email NOT NULL
bookings:     customer_id, class_id, block_id NOT NULL
blocks:       class_id, start_date, end_date NOT NULL
classes:      name, day, venue NOT NULL

NOT NULL violation = Postgres error code 23502; existing try/catch blocks
surface a generic toast ("Error saving class." / "Error adding block.").

PARTIAL UNIQUE INDEX
--------------------
bookings_unique_active_per_block:
  CREATE UNIQUE INDEX ON bookings (customer_id, block_id) WHERE status != 'cancelled'

Prevents duplicate non-cancelled bookings by same customer on same block, at DB level.
Re-booking after cancellation still works — cancelled rows excluded.
book_if_available RPC catches unique_violation and re-raises as ALREADY_BOOKED
so the front-end handles it identically.

SECURITY DEFINER FUNCTIONS
--------------------------
These bypass RLS and are called from the JS client instead of direct table access:

- lookup_customer(p_email) — returns customer record by email
- upsert_customer(p_first_name, p_last_name, p_email, p_phone, p_customer_type)
    inserts or updates customer, returns id
- book_if_available(p_block_id, p_class_id, p_customer_id, p_amount_due)
    atomic capacity check + booking insert, returns booking id
- check_priority_access(p_email, p_block_id) → BOOLEAN
    checks customer_class_priority (per-class manual) then previous-block
    confirmed booking
- has_active_booking_on_block(p_customer_id, p_block_id) → BOOLEAN
    true if customer has a non-cancelled booking on this block.
    Used by the booking modal step-1 handler.
- get_schema_fingerprint() → (fingerprint TEXT, total_objects BIGINT)
    Read-only deterministic hash of the public schema's structure (columns,
    constraints, indexes, RLS policies, functions, triggers, grants).
    Self-excluded from its own fingerprint. Used by the schema-check helper.

All functions granted EXECUTE to anon and authenticated roles.

DATABASE TRIGGERS
-----------------
trg_sync_block_booked_count — fires AFTER INSERT OR DELETE on bookings.
Calls sync_block_booked_count() which recalculates blocks.booked from actual
bookings (excluding cancelled). SECURITY DEFINER, SET search_path = public.

IMPORTANT: This trigger fires on app-level inserts/deletes only. If bookings
are bulk-modified via raw SQL, the booked count will NOT update automatically.
After raw SQL changes to bookings, run this resync:

    UPDATE blocks b
    SET booked = (
      SELECT COUNT(*) FROM bookings
      WHERE block_id = b.id AND status != 'cancelled'
    );

RLS POLICIES
------------
RLS enabled on all tables.

classes:                 SELECT anon+auth | INS/UPD/DEL auth only
blocks:                  SELECT anon+auth | INS/UPD/DEL auth only
bookings:                INSERT anon+auth (routes via SECURITY DEFINER) | SELECT/UPD/DEL auth only
customers:               INSERT anon+auth (routes via SECURITY DEFINER) | SELECT/UPD/DEL auth only
parq:                    INSERT anon+auth | SELECT/DEL auth only (no UPDATE — write-once)
settings:                SELECT anon+auth | INS/UPD auth only
waitlist:                ALL auth only
cancellations:           ALL auth only
customer_class_priority: ALL auth only

NOTE: customers and bookings have no anon SELECT policy — intentional. All
public reads of customer/booking data must go via SECURITY DEFINER functions.

ANON ROLE GRANTS
----------------
Table grants to anon:
  classes   — SELECT only
  blocks    — SELECT only
  settings  — SELECT only
  parq      — INSERT only (PAR-Q save during booking)

Tables with NO anon grants at all:
  bookings, customers, cancellations, waitlist, customer_class_priority

Function EXECUTE grants to anon:
  book_if_available, check_priority_access, has_active_booking_on_block,
  lookup_customer, upsert_customer, get_schema_fingerprint

The `authenticated` role retains ALL on every table (admin dashboard unaffected).

============================================================
5. FRONT-END
============================================================

CSS VARIABLES
-------------
--cream: #eef5f5 | --warm: #f7fafa | --sage: #3a8a8a | --sage-dk: #2a6b6b
--sage-lt: #b8d8d8 | --clay: #e07b4a | --charcoal: #1a2e2e | --mid: #4a6060
--light: #8aabab | --border: #cde0e0 | --green: #3a8a6a | --red: #c04040

STYLING GUIDE
-------------
Fonts:
  - Headings, logo, large numbers: Raleway (300, 400, 500, 600)
  - Body text, labels, buttons: Open Sans (300, 400, 500)
  - Class cards (new design): Barlow (700, 800) — loaded via Google Fonts
  - Tabler Icons webfont loaded via cdn.jsdelivr.net for card map-pin icon

Border radius:
  - Standard: 4px (sharp — modals, inputs, buttons)
  - Cards: 12px (new card design)
  - Pills/badges only: 100px
  - Circular avatars/icons: 50%

Spacing:
  - Page padding: 40px horizontal desktop / 18px mobile
  - Card padding: handled per-section (header 20px 22px, meta 14px 22px, etc.)
  - Modal body padding: 20px 28px

Buttons:
  Primary:        var(--sage) bg, white, border:none, Open Sans 12-14px,
                  letter-spacing 0.1em, text-transform uppercase, border-radius 4px,
                  padding 14px — NOTE: changed from charcoal to sage in card redesign
  Secondary/back: transparent bg, 1px solid var(--border), var(--mid)
  Sage variant:   var(--sage-dk) bg (admin export buttons)
  Full/disabled:  background #f0b0b0, color #7a1a1a (light red for full blocks)

CLASS CARD DESIGN (redesigned Session 38)
-----------------------------------------
Cards now use a structured section layout (not flat padding):
  - Header (.card-when): time column | vertical divider | class name + day row
    - Time column: two Barlow 700 18px teal time values stacked
    - Class name: .card-when-name — Barlow 800 22px uppercase charcoal
    - Day row (.card-when-day-row): day name (.card-when-day, Barlow 700 17px teal)
      + town pill (.card-loc-pill, teal pill showing c.loc e.g. "Baildon")
  - Meta row (.card-meta-row): venue with map-pin icon, spaces left with dot
    - Spaces dot colours: green (#3a8a6a) = available, orange (clay) = ≥80% full,
      red (#c04040) = full
    - .badge.b-full rendered inline in spaces row when full (visible, light red)
  - Block section (.card-block-section): "Current Block" label, date range (15px),
    date pills (13px teal pills, past=grey, today=dark teal)
    - Sessions remaining (pro-rata) shown below pills in muted text
  - Price row (.card-price-row): price + block length left, book button right
    - flex-grow:1 on price row pushes next block toggle to card bottom
  - Next block toggle (.next-blk-toggle): collapsible, background #e8f0f0
  - Card border: 1.5px solid #a8c8c8 (darker than before for card separation)
  - Card is display:flex flex-direction:column so all cards in a row are equal height

PLAYWRIGHT SELECTOR HOOKS (hidden elements, must not be removed)
-----------------------------------------------------------------
These elements are display:none but required by the Playwright test suite:
  - .card-loc          — contains c.loc (e.g. "Baildon") — used by SD-02/03/04
  - .cap-txt           — contains "N of M spots taken" — used by EC-10/11, CB-07
  - .badge.b-full      — visible (not hidden) inline in spaces row when full — EC-01
  - .priority-info     — class on locked window and priority window divs — PB-01/02
  - .card-when-day     — must contain c.day (day name) NOT c.name — all CB/BW/PB/EC specs
  - Book button labels: "Book Current Block" and "Book Next Block" (not "Book Now") — booking-flow.js

Booking modal:
  - .modal: max-width 500px, max-height 92vh, overflow-y auto (single scroll container)
  - .mhead: position sticky, top 0, z-index 10, background var(--warm) — sticky header
  - .mbody: padding 20px 28px — plain wrapper, NOT the scroll container
  - Step pips: 4 for new clients / 2 for returning clients

  CRITICAL: Do NOT convert .modal to a flex container — this breaks mobile scroll.

SHARED JS HELPERS
-----------------
- calcBlockDates(startDateStr, weeks) — builds dates array in "D MMM" format
- sanitise(str) — HTML-escapes strings for safe DOM insertion
- formatDateDisplay(isoStr) — formats YYYY-MM-DD → "D MMM YYYY" for display
- renderDatePills(dates) — sanitised date pill HTML
- populateBankDetails(name, sort, acc) — updates all bank detail DOM elements
- validateBlockOverlap(classId, startStr, weeks, excludeBlockId) — checks overlaps
- PARQ_LABELS / PARQ_KEYS — 12-element shared constants
- calcProrata(blk) — sessions remaining, pro-rata price, full price
- showAlreadyBookedView() — renders the duplicate-booking early-detection view

COMPLETED FEATURES
------------------
Client-facing:
- Class cards — redesigned (Session 38): Barlow font, time|divider|name layout,
  town pill, clean meta rows, no capacity bar, teal book button, collapsible next block
- Date pills: past/today/upcoming awareness
- Pro-rata pricing (sessions remaining shown below date pills)
- Next block collapsible toggle with priority/standard/locked icons
- Booking modal — 4 steps new / 2 steps returning
- Already-booked early detection view (returning client step 1)
- Priority booking system — priority 8-14 days before block start, standard 0-7 days

Admin dashboard (password protected via Supabase Auth):
- Full sidebar redesign (Session 39): dark charcoal sidebar, 8-page layout
  replacing old horizontal tab + below-tab sections structure
- All Bookings page — with search filter (client name / class)
- By Class page — accordion grouped by LOCATION (Baildon/Guiseley), then class
  Block rows styled as distinct cards with green/amber colour coding by status
- Clients page — with search filter (name / email / phone)
- Cancellations page — with search filter, Export CSV in topbar
- Classes page — table with active/upcoming block badges, warning banners below topbar
- Reports page — 4 summary stat cards, class capacity bars, client breakdown donut
- Settings page — two separate cards (bank details / notification email)
- Backup & Export page — per-table export rows + full backup card
- Block warnings banner renders below topbar on every page when warnings exist
- Test-mode env switch (URL query param ?env=test)

ADMIN DASHBOARD — SIDEBAR NAVIGATION
--------------------------------------
The admin dashboard now uses a sidebar layout (not tabs). Key selectors:

  Sidebar nav items (click to switch page):
    #dbnav-bookings, #dbnav-byclass, #dbnav-clients, #dbnav-cancellations
    #dbnav-classes, #dbnav-reports, #dbnav-settings, #dbnav-backup

  Active nav item has class .on:  #dbnav-bookings.on

  Page panels (shown/hidden):
    #dbpage-bookings, #dbpage-byclass, #dbpage-clients, #dbpage-cancellations
    #dbpage-classes, #dbpage-reports, #dbpage-settings, #dbpage-backup

  Active page panel has class .on: #dbpage-bookings.on

  Navigation function: switchDashPage(page)
    e.g. switchDashPage('classes') — switches sidebar item and page panel
    switchTab(tab) is retained as a shim mapping old tab names to new page names.

  Mobile: sidebar hidden, bottom nav bar shown (.db-bottom-nav)
    Bottom nav pins: bookings, clients, classes, reports, more (→ settings etc.)

  Tables inside dashboard pages:
    #btbody (All Bookings), #customers-tbody (Clients),
    #cancellations-tbody (Cancellations), #ctbody (Classes),
    #classes-accordion (By Class)

IMPORTANT PLAYWRIGHT NOTE — ADMIN DASHBOARD NAVIGATION:
  All admin specs must navigate to the correct page before asserting on
  page-specific elements. loginAsAdmin() lands on All Bookings (#dbnav-bookings.on).
  Specs that need Classes page must call:
    await page.locator('#dbnav-classes').click();
    await expect(page.locator('#dbnav-classes.on')).toBeVisible();
  before accessing #ctbody or the + Add New Class button.
  Similarly for Settings (#dbnav-settings), Backup (#dbnav-backup),
  Clients (#dbnav-clients), Cancellations (#dbnav-cancellations).

  #ctbody rows are hidden when the page is not on Classes — toBeVisible() fails.
  The saveSettings() button appears TWICE (bank details + email cards) — use
  .first() for bank details, .nth(1) for notification email.
  Export CSV appears on multiple pages — use onclick selector to disambiguate:
    exportCancellations() / exportTable('classes') / exportTable('bookings') etc.

ADMIN DASHBOARD — CSS CLASSES
-------------------------------
Layout:   .db-layout, .db-sidebar, .db-main, .db-topbar, .db-warnings, .db-content
Nav:      .db-nav-item (.on = active), .db-nav-section
Pages:    .db-page (.on = visible)
Stats:    .db-stat-grid, .db-stat-card, .db-stat-lbl, .db-stat-val
Cards:    .db-section-card, .db-settings-card, .db-export-row
By Class: .class-group-loc-header (location header), .class-group (class accordion)
          .blk-badge-active (green), .blk-badge-upcoming (amber)
Warnings: .db-warnings (wrapper below topbar), #block-warnings (inner div, display:none when empty)
Mobile:   .db-bottom-nav, .db-bn-item (.on = active)

CLASSES (PRODUCTION — IDs 28–32)
---------------------------------
28: Mixed Ability, Mon 9:45am–10:30am, Baildon Moravian Church, Baildon
29: Intermediate,  Mon 6:30pm–7:15pm,  St Johns Parish Church,  Baildon
30: Mixed Ability, Tue 9:45am–10:30am, Starlight Academy,       Guiseley
31: Beginner,      Wed 7:00pm–7:45pm,  Baildon Moravian Church, Baildon
32: Beginner,      Fri 10:30am–11:15am, Potting Shed,           Guiseley

BOOKING WINDOW LOGIC
--------------------
- >14 days before block start: LOCKED — timeline shown
- 8-14 days before:            PRIORITY WINDOW — priority clients can book
- 0-7 days before:             STANDARD WINDOW — all can book

============================================================
6. PLAYWRIGHT TEST SUITE
============================================================

PURPOSE
-------
Automated regression pack for the booking system. Runs against the isolated
lg-pilates-test Supabase project so real client data is never touched.

REPO LAYOUT
-----------
tests-playwright/
├── package.json                 # scripts: test, test:headed, test:ui, test:debug, report, seed, schema-check
├── playwright.config.js         # always-on trace/screenshot/video
├── .env.test.example            # template, committed
├── .env.test                    # secrets, gitignored — TEST_APP_URL, TEST_SUPABASE_DB_URL,
│                                #   TEST_ADMIN_PASSWORD, PROD_SUPABASE_URL, PROD_SUPABASE_ANON_KEY
├── README.md
├── TEST-PLAN.md                 # plain-English description of every test (updated each batch)
├── migrations/                  # SQL migrations applied to test DB
│   ├── 01_tables.sql
│   ├── 02_foreign_keys.sql
│   ├── 03_indexes.sql
│   ├── 04_functions.sql
│   ├── 05_trigger.sql
│   ├── 06_rls_and_grants.sql
│   ├── 07_seed_data.sql
│   ├── 08_seed_cb31_active_booking.sql    # obsolete — superseded by 09; do not re-run
│   ├── 09_reseed_with_dynamic_dates.sql   # live migration — idempotent, dynamic dates
│   ├── 10_add_schema_fingerprint_function.sql  # applied to BOTH prod and test
│   ├── 11_add_locked_window_class.sql     # Session 16 — Thu class for PB-01 locked window
│   ├── 12_reseed_resets_payment_mode.sql  # Session 44 — resets payment_mode on reseed
│   └── README.md
├── scripts/
│   ├── seed.js                  # npm run seed wrapper (safety checks + verification)
│   ├── schema-check.js          # compares prod vs test schema fingerprint
│   └── SCHEMA-CHECK-README.md
└── tests/
    ├── safety-check.js
    ├── helpers/
    │   ├── supabase.js          # shared anon client (exports `sb`)
    │   ├── app-url.js           # APP_PATH helper
    │   ├── booking-flow.js      # CB test helpers (incl. agreeAndReserve)
    │   ├── fixture-lookup.js    # role-based block lookup (getBlockByRole)
    │   ├── admin-auth.js        # admin login + per-class panel UI helpers
    │   └── admin-db.js          # direct pg helper for admin-only tables
    │                            # Exports: grantManualPriority, removeManualPriority,
    │                            #          hasManualPriority, setBookingStatus,
    │                            #          resyncBlockBookedCount,
    │                            #          setBlockBookedCount,
    │                            #          deleteBookingsForCustomerOnBlock,
    │                            #          deleteCustomerCascade,
    │                            #          getParqByCustomerId
    ├── smoke-01-anon-reads.spec.js
    ├── smoke-02-anon-rpcs.spec.js
    ├── smoke-03-rls-enforcement.spec.js
    ├── smoke-04-ui-page-loads.spec.js
    ├── cb-*.spec.js             # 27 CB spec files, 33 tests (CB suite complete)
    ├── pb-*.spec.js             # 14 PB spec files covering 10 Excel scenarios + 5 PB-X gap-analysis tests
    ├── sd-*.spec.js             # 6 SD spec files covering 6 of 6 scenarios
    ├── acl-*.spec.js            # 2 ACL spec files covering 2 of 2 genuine scenarios
    ├── bw-*.spec.js             # 3 BW spec files covering 3 of 3 genuine scenarios
    ├── sec-*.spec.js            # 3 SEC spec files covering 3 of 3 genuine scenarios
    ├── ec-*.spec.js             # 13 EC spec files covering 13 of 13 genuine scenarios
    ├── blw-*.spec.js            # 8 BLW spec files covering 8 of 8 genuine scenarios
    ├── se-*.spec.js             # 17 SE spec files covering 17 scenarios
    ├── st-*.spec.js             # 9 ST spec files covering ST-01 to ST-08 (PM-1 + PM-2)
    ├── ab-*.spec.js             # 13 AB spec files covering 21 of 21 genuine scenarios
    └── ac-*.spec.js             # 24 AC spec files covering 24 of 24 scenarios

CURRENT TEST COUNT: 195 tests (all passing — Session 45 / PM-6 complete)
  Smoke: 14 tests (anon reads, RPCs, RLS enforcement, UI loads)
  CB:    33 tests covering 33 of 33 CB scenarios (CB suite complete)
  PB:    16 tests — 9 individual Excel-scenario specs + 7 gap-analysis tests
         (PB-03 covered by PB-10, no standalone spec. PB-X1 has 3 sub-tests,
          PB-X2 through PB-X5 have 1 each.)
  SD:    6 tests covering 6 of 6 SD scenarios (SD suite complete)
  ACL:   2 tests covering 2 of 2 genuine ACL scenarios (ACL suite complete)
  BW:    3 tests covering 3 of 3 genuine BW scenarios (BW suite complete)
  SEC:   6 tests in 3 spec files covering 3 of 3 genuine SEC scenarios
  EC:    15 tests in 13 spec files covering 13 of 13 genuine EC scenarios
  BLW:   10 tests covering 9 of 9 BLW scenarios (BLW suite complete)
  SE:    20 tests covering 20 SE scenarios
  ST:    23 tests in 19 spec files — ST-01 to ST-08 + ST-16 (PM-1/PM-2),
         ST-17 to ST-26 (PM-6). ST-09 to ST-15 remain open placeholders
         (see TEST-PLAN.md Stripe Coverage Tracker note).
  AB:    21 tests in 13 spec files covering 21 of 21 genuine AB scenarios
  AC:    26 tests in 24 spec files covering 24 of 24 AC scenarios (AC suite complete)

CB-12 is covered by CB-01 (the new-client happy path includes T&Cs agreement).
The Coverage Tracker shows it as ✅ for that reason — no separate spec file exists.
PB-03 is covered by PB-10 (confirmed booking on previous block grants priority).

Coverage Tracker is in tests-playwright/TEST-PLAN.md at the top — that file
is the authoritative view of automation progress for all CB and PB scenarios.

THE SAFETY CHECK (non-negotiable)
----------------------------------
tests/safety-check.js is loaded by playwright.config.js BEFORE any test runs.
It throws and crashes the run if:
  1. TEST_SUPABASE_PROJECT_ID is missing
  2. TEST_SUPABASE_PROJECT_ID equals production (mrlooyixnlxzcfmvnqme)
  3. TEST_SUPABASE_URL contains the production project reference
  4. TEST_SUPABASE_ANON_KEY is missing
  5. The anon key's JWT 'ref' claim is the production project ID

LIMITATION: This safety check only guards the test helper's Supabase client.
It does NOT guard the browser page's own Supabase client. The TEST MODE banner
assertion in CB test beforeEach() hooks is the second line of defence.

TEST FIXTURE — DYNAMIC DATES
----------------------------
Migration 09 dynamically computes all block dates from CURRENT_DATE at the
moment the migration runs, so the fixture always stays correctly positioned
relative to "today" — priority window is always priority, standard is always
standard, etc.

Migration 09 is idempotent and re-runnable. Running it again wipes the
existing fixture and rebuilds it fresh. Migration 11 (added Session 16)
adds the Thursday class for PB-01 — also idempotent. `npm run seed` now
applies 09, 11, and 12 in sequence inside a single transaction.

CURRENT TEST FIXTURE — 11 blocks across 4 classes:

  Mon Mixed (class_id=1):
    - Mon past         (completed, priority-source)
    - Mon current      (active, CB-31 duplicate booking)
    - Mon upcoming     (priority window, ~+8 to +13 days out, PB-09/PB-10 target)
    - Mon full         (upcoming, ~+50 days out, cap=2, FULL)

  Wed Beginner (class_id=2):
    - Wed past         (completed, priority-source)
    - Wed upcoming     (priority window, ~+8 days out)

  Thu Mixed (class_id=4) [Migration 11]:
    - Thu current      (active, anchor for the card so a nextBlk panel renders)
    - Thu locked       (upcoming, ~+30 days out — drives PB-01 locked window UI)

  Fri Intermediate (class_id=3):
    - Fri old past     (completed, older history)
    - Fri recent past  (completed, just-finished, priority-source)
    - Fri upcoming     (standard window, ~+3 days out)

Customers (3):
  - returning-one@test.example — confirmed on Mon past, Mon current, Wed past
                                  + manual priority on Wed class
  - returning-two@test.example — confirmed on Mon past, Fri recent past, Mon full
  - admin-dummy@test.example   — confirmed on Mon full (fills the cap-2 slot)

Bookings total: 7. Priority grants: 1. (Migration 11 adds NO customers/bookings.)

ROLE-BASED BLOCK LOOKUP
-----------------------
tests/helpers/fixture-lookup.js exports getBlockByRole(role) and
getBlocksByRoles([roles]). Specs MUST use these helpers rather than
hardcoding block IDs — IDs regenerate on every reseed.

Valid roles (11):
  mon-past, mon-current, mon-upcoming, mon-full,
  wed-past, wed-upcoming,
  thu-current, thu-locked,
  fri-old-past, fri-recent-past, fri-upcoming

Lookup strategy: one SELECT query at first call, cached for rest of test run.
Throws FixtureDriftError with "Run npm run seed" guidance on drift.

NPM RUN SEED
------------
`npm run seed` from ~/dev/lg-pilates-booking/tests-playwright reseeds the
test DB by re-applying migration 09.

Behaviour:
- Refuses to run if TEST_SUPABASE_PROJECT_ID or TEST_SUPABASE_DB_URL
  references production
- Prompts for "yes" confirmation (skip with `npm run seed -- --yes` for CI)
- Runs the entire migration in a BEGIN/COMMIT transaction
- Verifies post-run: 11 blocks, 4 classes, 3 customers, 7 bookings, 1 priority

Requires in .env.test:
  - TEST_SUPABASE_DB_URL — direct Postgres connection string for test DB
    (session pooler URI from Supabase dashboard → Project Settings → Database)

TEST DATA FRESHNESS — DRIFT CHECK
----------------------------------
The fixture eventually drifts as "today" moves on. After ~1 week without
reseeding, window states start shifting (priority becomes standard, active
becomes completed, etc.).

Claude runs a drift check at the start of every session (see WORKFLOW
section). If drift is detected, Claude reminds Mark to run:

    cd ~/dev/lg-pilates-booking/tests-playwright
    npm run seed

ALWAYS-ON TEST EVIDENCE
-----------------------
playwright.config.js use block: trace: 'on', screenshot: 'on', video: 'on'

Every test run produces a full trace file, screenshots per step, and a video.

Viewing options:
  npx playwright show-report        # HTML report after any `npm test` run
  npm run test:ui                   # interactive UI runner with step timeline
  npx playwright show-trace <file>  # view a specific trace zip

Disk usage: ~20-50MB per full test run in test-results/. Overwrites each run.

TEST / PROD SCHEMA PARITY
--------------------------
Both projects have a SECURITY DEFINER function get_schema_fingerprint()
that returns a deterministic MD5 hash + object count covering: columns,
constraints, indexes, RLS policies, functions, triggers, and grants in
the public schema. Read-only, granted to anon, exposes nothing sensitive.

scripts/schema-check.js calls the RPC on both projects via their anon keys
and compares the results. Auto-generated constraint and index names are
normalised so cosmetic differences don't trigger a false positive.

To run:
    cd ~/dev/lg-pilates-booking/tests-playwright
    npm run schema-check

Expected output when in sync:
    PROD: dad311c8a152bc0dbec1cb3fa0c584d5  (248 objects)
    TEST: dad311c8a152bc0dbec1cb3fa0c584d5  (248 objects)
    ✅ Schemas are identical.

When to run:
- After applying any schema change to production
- At the start of a session if you suspect drift
- Before a release or deployment milestone
- Quarterly as a routine health check

MAC SETUP
---------
- Node.js v24.15.0 (via nodejs.org LTS)
- npm v11.12.1
- Homebrew 5.1.7
- GitHub CLI (gh) 2.90.0 authenticated as mjones2420-netizen (HTTPS)
- Git 2.50.1 (Apple Git-155)
- Git user.name="Mark Jones", user.email="mjones2420@gmail.com"
- Repo cloned locally at: /Users/markjones/dev/lg-pilates-booking
- Playwright scaffold at: /Users/markjones/dev/lg-pilates-booking/tests-playwright
- .env.test in place (gitignored)
- Local server: python3 -m http.server 8000 (from repo root)
- TEST_APP_URL=http://localhost:8000/?env=test
- devDependencies: @playwright/test, @supabase/supabase-js, dotenv, pg
- Supabase CLI v2.105.0 (installed via Homebrew, Session 33)
- Docker Desktop installed (required by Supabase CLI for Edge Function bundling, Session 33)
- Supabase CLI login: use `export SUPABASE_ACCESS_TOKEN=...` if keychain blocks `supabase login`
  (Token created at https://supabase.com/dashboard/account/tokens)

RUNNING TESTS
-------------
Terminal tab 1 (keep running):
    cd ~/dev/lg-pilates-booking
    python3 -m http.server 8000

Terminal tab 2:
    cd ~/dev/lg-pilates-booking/tests-playwright
    npm test                        # run all tests, generate HTML report
    npx playwright show-report      # open last report in browser
    npm run test:ui                 # interactive UI runner (RECOMMENDED for Mark)
    npm test -- --headed            # show browser during run
    npm test -- --headed --slow-mo 500
    npm run seed                    # reseed test DB (prompts for yes)
    npm run seed -- --yes           # skip prompt (CI)
    npm run schema-check            # verify prod/test schema parity

TEST DATA ACCUMULATION
----------------------
Most specs self-clean via afterEach (deleteCustomerCascade or
deleteBookingsForCustomerOnBlock). A small number of older CB specs use
entry-side cleanup instead. If stray test customers accumulate over many
runs, the cleanest reset is `npm run seed`.

Manual cleanup SQL (test DB only):
  DELETE FROM parq WHERE customer_id IN (SELECT id FROM customers WHERE email LIKE 'cb%-%@test.example');
  DELETE FROM bookings WHERE customer_id IN (SELECT id FROM customers WHERE email LIKE 'cb%-%@test.example');
  DELETE FROM customers WHERE email LIKE 'cb%-%@test.example';
  UPDATE blocks b SET booked = (SELECT COUNT(*) FROM bookings WHERE block_id=b.id AND status != 'cancelled');

============================================================
7. WORKFLOW
============================================================

SESSION START — CLAUDE'S CHECKS
-------------------------------
Run ALL THREE of these EVERY session, regardless of whether the session
looks test-related:

A. Confirm index.html is present
   - Check line count (~4,023 lines post-Session 44)
   - Copy to /home/claude/index.html as the working file

B. Check test DB fixture TIME drift
   - Run this query against project ID ngzfhamjuviwfwuncrjo. It returns
     one row per active/upcoming block with a `drift_warning` flag for
     blocks sitting on the exact boundary of their expected window.

       SELECT
         id, class_id, status, start_date,
         (start_date - CURRENT_DATE) AS days_until_start,
         CASE
           WHEN status = 'active'   AND end_date < CURRENT_DATE + 1                       THEN 'about to complete'
           WHEN status = 'upcoming' AND (start_date - CURRENT_DATE) = 8                   THEN 'about to leave priority window'
           WHEN status = 'upcoming' AND (start_date - CURRENT_DATE) = 15                  THEN 'about to enter priority window'
           WHEN status = 'upcoming' AND (start_date - CURRENT_DATE) = 1                   THEN 'about to become active'
           ELSE NULL
         END AS drift_warning
       FROM blocks
       WHERE status IN ('active','upcoming')
       ORDER BY class_id, start_date;

   - Healthy fixture: every row has drift_warning = NULL.

C. Check test DB fixture STATE drift
   - Run this query against project ID ngzfhamjuviwfwuncrjo:

       SELECT
         (SELECT COUNT(*) FROM customers
          WHERE email LIKE 'cb%-%@test.example'
             OR email LIKE 'pb%-%@test.example')                AS stray_test_customers,
         (SELECT COUNT(*) FROM blocks
          WHERE status IN ('active','upcoming')
            AND booked >= cap AND cap > 2)                      AS unexpectedly_full_blocks;

   - Healthy: stray_test_customers low single digits, unexpectedly_full_blocks = 0

   Report combined result for B + C as a single line near the top of the
   opening response.

DURING SESSION
--------------
- Make all changes to /home/claude/index.html
- Copy final output to /mnt/user-data/outputs/index.html for Mark to download
- If any Playwright test is added or substantially modified, TEST-PLAN.md
  MUST be updated in the same session. Non-negotiable.
- When updating TEST-PLAN.md after adding tests, keep the header suite table
  in sync: update the spec file count and test count for the affected suite,
  update the per-tab <summary> header to "X spec files, Y tests ✅", and add
  the new spec file(s) to the collapsible full spec file list.

FEATURE DEVELOPMENT FLOW
------------------------
Every index.html change follows this loop without exception:

1. Upload latest index.html to start the session
2. Mockup produced → Mark signs off
3. Code change made in session
4. New Playwright specs written in the same session (see rule below)
5. Download updated index.html → replace ~/dev/lg-pilates-booking/index.html
6. Run `npm test` locally — this reseeds the DB automatically, then runs
   the full suite including the new specs
7. If all tests pass → git push
8. GitHub Actions runs the suite again on push as a safety net

HARD RULE: No git push until local `npm test` is green, including any
new specs for the new feature.

NEW SPECS RULE: New or changed functionality always gets new Playwright
specs written in the same session as the code change — not deferred to
a follow-up session. The only exception is a purely cosmetic fix where
no behaviour changes and no spec is warranted (e.g. a label typo fix).

TEST-PLAN.md must be updated in the same session as any new spec.

END OF SESSION — FILES TO UPDATE
---------------------------------
At the end of every session, check whether each of these files needs updating
— not just context.txt. Claude should flag which ones are affected before
producing output files.

- context.txt              — always update
- BACKLOG.md               — if a feature advanced, completed, or a new item was identified
- EMAIL-NOTIFICATIONS-SPEC.md — if an email session completed or spec details changed
- TEST-PLAN.md             — if any tests were added or modified (non-negotiable companion step)
- LG_Pilates_Test_Scenarios.xlsx — if scenario wording changed

AFTER SESSION — MARK'S STEPS
----------------------------
1. Download the output index.html (and any new spec files) from Claude
2. Replace ~/dev/lg-pilates-booking/index.html with the downloaded file
3. Copy any new spec files into tests-playwright/tests/
4. Run `npm test` locally — confirm all tests pass before pushing
5. Push to GitHub:

     cd ~/dev/lg-pilates-booking
     git status                    # confirm only expected files are modified
     git add index.html context.txt
     git commit -m "TITLE"
     git push

   Multi-line commit messages with em-dashes or backticks can get tangled
   in zsh quoting. Best to use a SHORT title only.

   Claude's commit-message format: short single-line title.

FILE DRIFT PREVENTION
---------------------
- index.html is uploaded manually at session start — never use a cached version
- If a second index.html is uploaded mid-session, warn Mark and confirm which to use
- Always push the output file to GitHub before ending or starting a session
- Use meaningful commit messages on every push

BACKLOG
-------
BACKLOG.md at the repo root is the single source of truth for all outstanding
features and tasks. It is organised by priority tier and links to detailed
descriptions for each item.

- At the start of any feature session, consult BACKLOG.md to confirm the
  item being worked on and check for dependencies.
- If a new feature idea or task comes up during a session that is not being
  actioned immediately, add it to BACKLOG.md before the session ends.
- When an item is completed, mark it ✅ in BACKLOG.md and move it to the
  Completed table. Update context.txt at the same time.
- BACKLOG.md should be committed to git whenever it is updated.

COMMUNICATION STYLE — MARK'S PREFERENCES
-----------------------------------------
- One action per response, then wait for Mark to confirm before the next
- Don't bury action requests at the end of long explanations
- Keep technical detail brief; plain English summaries only where needed
- Don't offer "I can do this next time" as a buried aside — if it matters,
  track it in context.txt or the TO DO list
- Shorter overall; less narrative; still helpful but focused
- Whenever a file is presented for download, always provide ready-to-copy
  git commands to commit and push it, even outside of a formal end-of-session

============================================================
8. TO DO (priority order)
============================================================

1. PLAYWRIGHT TESTS — ALL EXCEL SCENARIOS COMPLETE ✅

   All 133 genuine Excel scenarios across all 12 tabs are now automated.
   169 tests across 14 suites, all passing.

   Full per-suite detail and spec-to-scenario mapping lives in
   tests-playwright/TEST-PLAN.md (Coverage Tracker at the top).

   SUITE SUMMARY:
   - ✅ Client Booking (CB)     — 34 tests, 33 scenarios + CB-16b gap
   - ✅ Priority Booking (PB)   — 16 tests, 10 scenarios + 5 PB-X gap-analysis
   - ✅ Booking Windows (BW)    — 3 tests, 3 genuine scenarios
   - ✅ Admin Bookings (AB)     — 21 tests, 21 scenarios
   - ✅ Admin Classes (AC)      — 26 tests, 24 scenarios (Batches 18-20)
   - ✅ Admin Clients (ACL)     — 2 tests, 2 genuine scenarios
   - ✅ Schedule Display (SD)   — 6 tests, 6 scenarios
   - ✅ Settings & Export (SE)  — 16 tests, 16 scenarios
   - ✅ Stripe (ST)             — 9 tests, ST-01 to ST-08 (PM-1 + PM-2)
   - ✅ Edge Cases (EC)         — 15 tests, 13 genuine scenarios
   - ✅ Block Warnings (BLW)    — 8 tests, 8 scenarios
   - ✅ Security (SEC)          — 6 tests, 3 genuine scenarios
   - ✅ Smoke                   — 14 tests

   INFRASTRUCTURE BACKLOG:
   - ✅ GitHub Actions CI for auto-run on push (Session 32)
   - Mobile Safari Playwright project for proper mobile coverage
     (CB-29/CB-30 currently use shrunken desktop viewport at 480x700)

2. INDEX.HTML — minor cosmetic bug:
   Line 476 still reads "Step 1 of 3 — Your Details" — leftover from the
   old 3-step flow. Lines 501, 558, 576 correctly say "Step 2 of 4",
   "Step 3 of 4", "Step 4 of 4". The pip indicator itself is correct;
   only this in-app step-label text needs fixing.

3. EMAIL NOTIFICATIONS via Resend — Session 8 (final review) next
   Fully scoped. Spec file: EMAIL-NOTIFICATIONS-SPEC.md (repo root).
   Sessions 1–7 complete. All 6 email templates built and verified in production.
   Session 8 next: end-to-end production verification, mobile/desktop render
   check, forwarding to Louise's inbox.
   Build plan: 6 sessions + final review. See EMAIL-NOTIFICATIONS-SPEC.md.

4. PAYMENT MODE FEATURE FLAG + STRIPE INTEGRATION — Sessions PM-1 to PM-6 ✅ COMPLETE
   Fully scoped. Spec file: PAYMENT-MODE-SPEC.md (repo root).
   Allows Louise to switch between bank transfer and Stripe Checkout from
   the admin Settings page. Bank transfer is the default at launch.
   PM-1 complete: feature flag foundation (settings UI, PAYMENT_MODE constant, Stripe badge).
   PM-2 complete: booking modal Step 4 branches on payment mode — bank transfer
   path unchanged; Stripe path shows new explainer and Proceed to Payment button.
   PM-3 complete: Stripe Checkout integration. stripe-checkout Edge Function deployed.
   pending_bookings table created on both DBs. Booking flow redirects to Stripe without
   saving a real booking. Payment success shows a full confirmation screen (not a toast).
   Stripe account: Mark's account, GBP, test keys in use.
   PM-4 complete: stripe-webhook Edge Function deployed to test AND production.
   Verified end-to-end on both — creates real booking (status=confirmed,
   stripe_payment_intent_id + stripe_checkout_session_id populated), saves PAR-Q
   if new client, sends confirmation emails (triggers 2 and 5S), plus an admin
   failure-alert email if payment succeeds but booking can't be placed
   (CLASS_FULL/ALREADY_BOOKED — flags for manual refund, pending_bookings row
   retained for manual review in that case).
   PM-5 complete: stripe-webhook deployed to PRODUCTION (v3). Production Stripe
   webhook endpoint created (lg-pilates-prod-webhook), listening for
   checkout.session.completed, STRIPE_WEBHOOK_SECRET set on production Edge
   Function. End-to-end test successful — booking 227 confirmed with both
   Stripe IDs populated. Both environments share the same Stripe account
   (test mode keys) — production webhook is also in Stripe test mode until
   live keys are swapped in at go-live.
   Client confirmation email (buildConfirmedEmailHtml) updated to show
   individual session date pills instead of a date range — applied to
   index.html AND both stripe-webhook deployments (test + prod, v3).
   PM-6 complete (Session 45): 10 new Playwright specs (ST-17 to ST-26, 14 tests)
   covering the Stripe Checkout + webhook flow end-to-end — pending_bookings
   creation, cancel-redirect preservation, webhook success (booking confirmed
   with Stripe IDs, PAR-Q for new clients), email-template content checks,
   webhook failure paths (CLASS_FULL, ALREADY_BOOKED), invalid signature
   rejection, and duplicate-delivery handling. See SESSION 45 LEARNINGS below
   and TEST-PLAN.md's Stripe Coverage Tracker for full detail.

   PRODUCTION STATE (as of Session 45 — IMPORTANT):
   - Production payment_mode is currently 'stripe' (set deliberately this
     session — the booking system is not yet live, so this is safe). Confirm
     intended mode before go-live.
   - The 4 leftover pending_bookings rows on production (from PM-5 testing)
     were cleared this session.

   PAYMENT MODE COMMUNICATION RULES (apply to ALL PM sessions):
   - Before each session: explain the aim and objectives in plain English
   - Keep technical jargon to a minimum throughout
   - One step at a time — never move to the next step without explicit confirmation
   - Always explain what something does in plain English alongside any technical detail
   - If a concept needs explaining (e.g. "what is a webhook"), explain it briefly
     before using it, without being asked

5. REPORTS PAGE — monthly trend chart (Session C)
   Reports page static stats are complete. Next: add monthly bookings/revenue
   chart requiring a new Supabase query grouping bookings by month.

6. CLASS REGISTER FEATURE
7. DEMO FILE UPDATES (multiple outdated since per-class priority + pro-rata + 4-step flow)
8. HONEYPOT anti-bot protection
9. BANK DETAILS — replace placeholder with Louise's real details
10. HOSTING MIGRATION & CUSTOM DOMAIN (GitHub Pages → Netlify, book.lg-pilates.co.uk)
11. USER GUIDE — update PDF at post-email milestone
12. WAITLIST FEATURE — table exists, not yet connected
13. FILE SPLIT — consider at next major milestone
14. AUDIT ITEMS:
    - Swap Supabase anon key to newer publishable key format (at Netlify migration)
    - Enable leaked password protection in Supabase Auth dashboard (manual toggle)
    - Edge Function parity check: no automated check compares deployed Edge
      Function code between prod and test projects (only DB schema is checked
      via schema-check). Caused stripe-checkout v2 to be deployed to prod only
      in Session 42, undetected until Session 43. Consider extending
      schema-check or adding a manual checklist item.
    - Missing migration files for pending_bookings table (Session 42) and
      bookings.stripe_payment_intent_id/stripe_checkout_session_id columns
      (Session 43) — both applied via apply_migration MCP directly, no file
      saved to tests-playwright/migrations/. Create these so file-on-disk
      matches DB-state.

============================================================
8a. PAYMENT SYSTEM — REMAINING ITEMS (pre go-live)
============================================================

With PM-6 complete, the payment system build is done. Remaining items before
go-live (none urgent — system not yet public):

- Swap Stripe test keys for live keys (separate task, needs its own
  checklist — webhook secret will also change on both test and prod).
- Switch bookings@lg-pilates.co.uk forwarding from Mark's inbox to Louise's.
- Confirm/decide production payment_mode before go-live (currently 'stripe',
  set deliberately in Session 45 — see TO DO item 4).
- Migration files still missing for pending_bookings, the Stripe ID columns,
  and the stripe-checkout/stripe-webhook Edge Functions (TO DO item 14,
  AUDIT ITEMS).
- Netlify migration (TO DO item 10) is now unblocked — was deferred pending
  payment system completion.

============================================================

These are durable, hard-won lessons. Add new ones sparingly — only if
they'd save a future session real time.

DATABASE & SUPABASE
-------------------
- customer_type values must be lowercase: 'new', 'returning', 'vip' (or NULL).
- blocks.dates[] stores display-formatted strings ("6 Apr", "13 Apr"), NOT ISO dates.
- Front-end JS must never rely on direct SELECT against customers or bookings
  from an anon context — RLS silently returns empty arrays rather than erroring.
- Three-layer defence against duplicate bookings (DB unique index, RPC catch,
  front-end early detection).
- PAR-Q presence is flagged in the dashboard for new-client bookings only.
- Supabase web SQL editor has a parser bug with PL/pgSQL DECLARE vars.
  Use apply_migration (MCP) or Supabase CLI for any DDL involving function bodies.
- supabase_migrations.schema_migrations stores full SQL of applied migrations
  in its `statements` column — useful for reconstructing migrations applied
  via MCP without a local file.
- Test admin users on Supabase MUST be created via the dashboard, not via raw
  SQL. Raw inserts into auth.users don't populate auth.identities, which
  Supabase Auth requires for password login.
- After raw SQL changes to bookings, manually resync blocks.booked — the
  trigger only fires on app-level inserts/deletes (see DATABASE TRIGGERS).
- The lg-pilates-test Supabase project is a byte-for-byte mirror of production
  schema. Any future production schema changes MUST be mirrored to test or
  tests will drift from reality.

PLAYWRIGHT / TEST INFRA
-----------------------
- Excel test scenarios live in LG_Pilates_Test_Scenarios.xlsx. As of
  Session 22 (15 May 2026) this file is committed to git at the repo root
  alongside index.html and context.txt — pushed and updated whenever
  scenarios change. Three gotchas: (a) the "Block Warnings" tab IDs were
  renamed BW-* → BLW-* on 10 May 2026 to disambiguate from the "Booking
  Windows" tab. So BLW-01 to BLW-08 are the dashboard banner tests; BW-01
  to BW-06 are the booking-window UI states. (b) Red-filled rows in the
  Excel file are duplicates of existing automated tests or housekeeping
  steps — do NOT pick these up in future batches. The TEST-PLAN.md
  Coverage Tracker is the source of truth for which Excel scenario maps
  to which automated spec. (c) Excel styling pattern: alternating row
  fills FFFFFFFF / FFEEF5F5 (white / cream), SQL column (column 5) uses
  Courier New 8pt with FFE8F0FE fill (pale blue). Preserve these when
  updating rows via openpyxl.
- tests/helpers/supabase.js exports `sb`, NOT `supabase`. Any new helper
  importing from it must use `const { sb } = require('./supabase')`.
- Spec helper imports use './helpers/...' (same level), NOT '../helpers/...'.
  Helpers live INSIDE tests/, not above it.
- Block IDs are NOT stable across reseeds — specs must look up by role
  via fixture-lookup.js, never by literal ID.
- Playwright's baseURL strips query strings from page.goto('/'). Always
  use the APP_PATH helper from tests/helpers/app-url.js.
- Every CB/AB/PB spec's beforeEach must assert #test-mode-banner.on before
  doing anything else — fail fast, fail loud. The browser-page Supabase
  client is NOT covered by the safety-check.js helper guard.
- openBookingModal helper signature is (page, day, which) — `day` is a
  string like "Monday", NOT a numeric class_id. `which` is 'current'
  (default) or 'next'.
- Button labels on class cards:
    "Book Current Block"  — active blocks AND upcoming when no active block
    "Book Next Block"     — only in standard window (0-7 days out)
    "Check My Priority"   — priority window (8-14 days out), no direct book
  IMPORTANT: These labels must not be changed — booking-flow.js clicks by
  exact text. The displayed button says "Book Current Block" not "Book Now".
- .card-when-day must contain the DAY NAME (e.g. "Monday"), not the class
  name. Class name is in .card-when-name. Swapping these breaks every spec
  that filters cards by day.
- .card-loc must remain as a hidden span containing c.loc — SD filter tests
  rely on it. The visible location pill (.card-loc-pill) is separate.
- .cap-txt must remain as a hidden span with "N of M spots taken" text —
  EC-10/11 and CB-07 assert on its text content.
- .priority-info class must remain on the locked window and priority window
  divs in the next block panel — PB-01/02 assert on it.
- Step pip mechanism: completed steps swap textContent from the number to
  a tick (\u2713). Assertions need toHaveText('\u2713'), not just a class
  check. The .done class is added but the visual change comes from BOTH.
- CB-13 uses test.skip() (not expect().toBe(false)) on its pre-flight
  already-booked check, so a successful first run leaves the suite green
  on the second run.
- Priority gate UI is rendered ONLY on a class card's nextBlk panel — a
  class needs both a current block AND an upcoming block for the gate to
  appear. In Migration 09 only Monday meets this (mon-current active +
  mon-upcoming). Wed and Fri have one upcoming block each which renders as
  "current" via getActiveBlock fallback — no nextBlk panel, no gate. Always
  verify the rendering path actually fires on the chosen card BEFORE writing
  a spec.
- check_priority_access RPC selects "previous block" as the most-recent
  end_date < the upcoming block's start_date — this can be an active block
  (mon-current is the previous block for mon-upcoming, NOT mon-past). When
  setting up priority specs, always run `check_priority_access` directly
  for the planned customer/block pair rather than reasoning about it from
  the bookings table alone.
- book_if_available RPC always inserts as status='reserved' and bypasses
  block status checks (only enforces capacity + duplicate detection). Useful
  for spec setup when you need a reserved booking on any block — including
  active blocks — without involving the admin login flow.
- Migration 11 adds the Thursday class for PB-01's locked-window UI. seed.js
  was extended to apply 09 + 11 in sequence inside one transaction. Any
  future fixture-affecting migrations must (a) be added to MIGRATION_FILES
  in seed.js and (b) update the EXPECTED counts. Smoke-01 also asserts
  exact totals — check that too on any fixture change.
- File-on-disk vs DB-state divergence is a real failure mode: Session 16
  applied a Tuesday→Thursday change via Supabase MCP but didn't update the
  saved migration file. Tests passed until the next reseed re-applied the
  stale file. Rule: when a change has both a "live apply" and a "file save"
  component, do them from the same source string.
- The shared `sb` client in tests/helpers/supabase.js is anon-only. It has
  NO grants on customer_class_priority, bookings, customers, cancellations,
  or waitlist. Calls against those tables return null/empty silently rather
  than erroring (RLS behaviour). For fixture writes/reads on admin-only
  tables, use tests/helpers/admin-db.js which opens a direct Postgres
  connection via TEST_SUPABASE_DB_URL and bypasses RLS.
- Admin login helper: tests/helpers/admin-auth.js drives the dashboard login
  UI via #dash-email / #dash-password. Requires TEST_ADMIN_EMAIL and
  TEST_ADMIN_PASSWORD in .env.test.
  loginAsAdmin() asserts #dbnav-bookings.on after login (NOT #tab-bookings.on).
  openClientsTab() clicks #dbnav-clients (NOT #tab-customers).
- Per-class panel button selectors use the onclick attribute pattern:
  `button[onclick="toggleClassPriority(custId,classId,true|false)"]`.
  Third arg is true=Remove (currently granted), false=Grant (not granted).
- When admin signs out via signOut(), show("schedule") only UN-HIDES the
  existing schedule DOM — it does NOT re-render. Any panels open before the
  admin session remain open after sign-out.
- The card.locator('.block-dates-pills .date-pill') pattern grabs pills
  from BOTH the current-block section AND the next-block panel. When
  asserting pill counts/order on a card with a next-block panel, scope
  with `.first()`: `card.locator('.block-dates-pills').first()
  .locator('.date-pill')`.
- fixture-lookup.js SELECT returns: id, class_id, status, start_date,
  end_date, cap, booked, dates. Any future spec needing other block columns
  (price, weeks, visible) should extend the SELECT.
- The admin dashboard has a SIDEBAR with 8 pages (not 4 tabs + below-tab
  sections). Page selectors: #dbpage-bookings, #dbpage-byclass,
  #dbpage-clients, #dbpage-cancellations, #dbpage-classes, #dbpage-reports,
  #dbpage-settings, #dbpage-backup. Navigation: switchDashPage(name).
- trg_sync_block_booked_count fires AFTER any INSERT or DELETE on the
  bookings table and recalculates blocks.booked. Any direct UPDATE to
  blocks.booked gets OVERWRITTEN the moment a real booking row is inserted.
  setBlockBookedCount is only safe for UI-state tests that do NOT cause any
  subsequent bookings INSERT/DELETE on the same block.
- The booking success view's overlay (#overlay.on) intercepts clicks on
  the Dashboard nav button afterwards. Specs that complete a booking and
  then need to log in as admin MUST click "Back to Schedule" before
  navigating elsewhere.
- The admin Bookings table (#btbody) renders 7 columns:
    Client (first + last, no email), Class, When, Block, Paid, Status, Actions
  Filter rows by customer name, not email.
- Duplicate-booking protection has THREE independent layers:
    (1) Front-end Step-1 early-detection (CB-31)
    (2) RPC layer ALREADY_BOOKED (EC-13, EC-08)
    (3) Partial unique index (EC-12)
- Transient UI state (EC-09's "Reserving..." button text) requires
  expect.poll with short intervals (50/100/200ms) and a small timeout (4s).
- fixture-lookup.js had been written before any spec needed the blocks.dates
  column. When BW-02 needed it, the field was absent from the SELECT and
  silently undefined. Rule: when a shared helper omits a column a new spec
  needs, extend the helper rather than a one-off direct query.
- The clean fixture already triggers the yellow advisory for Wednesday and
  Friday — each has only one visible block. Thursday is the reliable on/off
  switch for BLW tests: hiding thu-locked creates the advisory for Thursday
  without disturbing the Wed/Fri baseline.
- settings table uses `key` as the PRIMARY KEY (text). ON CONFLICT (key) DO
  UPDATE is the correct upsert pattern. There is no separate `id` column.
- openModal(classId, blockId) must only be called AFTER the schedule has
  rendered — i.e. after at least one `.card` is visible.
- goStep2() silently rejects the form if ANY of the four Step 1 fields
  are empty or invalid. Specs that want to test the returning-client payment
  step must fill ALL FOUR fields, not just email.
- The admin dashboard contains multiple tables (#btbody, #customers-tbody,
  #cancellations-tbody, #ctbody). Always scope to the specific table via
  its tbody ID when asserting headers.
- book_if_available RPC always inserts as status='reserved'. All fixture
  bookings are seeded as 'confirmed'. AB specs that need a reserved booking
  must create one per-run via the RPC.
- window.confirm dialogs must be handled with page.once('dialog', d =>
  d.accept()) registered BEFORE the triggering button click.
- AB-10 double-click collapse bug: always click the target group directly
  once, never combine a generic "expand first" click with a named-group click.
- Nav button for the public schedule is #nb-schedule, NOT #nb-home.
- Classes and blocks inserted via direct pg before page.goto ARE loaded into
  the app's in-memory arrays on page init. Insert before goto.
- toISOString() on a local midnight Date converts to UTC, shifting the date
  one day back in BST. For any date string passed into a browser date input,
  always use toLocalISO() (getFullYear/getMonth/getDate extraction).
- toggleClassGroup() uses style.display directly ('block'/'none'), NOT a
  CSS class toggle. Assert toBeVisible() not toHaveClass(/on/).
- parq table column names do NOT match the front-end question labels. Always
  verify column names against the DB schema first.
- The By Class page now groups classes by LOCATION (Baildon/Guiseley) before
  listing individual class accordions. The location header uses
  .class-group-loc-header. Each class within a location is a .class-group
  accordion. Block rows inside a class are styled as distinct cards
  (green background = active, amber = upcoming).
- The saveSettings() button appears TWICE on the Settings page (one per card).
  Use .first() for bank details, .nth(1) for notification email.
- Export CSV buttons are not unique — multiple buttons say "Export CSV".
  Always use onclick selectors: exportCancellations(), exportTable('classes'),
  exportTable('blocks'), exportTable('customers'), exportTable('bookings'),
  exportAll().
- BLW specs use #btbody tr (bookings table) as a readiness check after login,
  NOT #ctbody tr (classes table). #ctbody is only visible on the Classes page.

CARD REDESIGN SELECTOR DISCIPLINE (Session 38)
----------------------------------------------
The card redesign (Session 38) changed the HTML structure significantly.
Key discipline going forward: when making ANY change to card HTML, always
verify these five selector hooks remain intact and correct:
  1. .card-when-day contains c.day (day name string)
  2. .card-loc hidden span contains c.loc (location string)
  3. .cap-txt hidden span contains "N of M spots taken"
  4. .badge.b-full is VISIBLE (not display:none) when block is full
  5. .priority-info class present on locked/priority window divs
  6. Book button labels: "Book Current Block" / "Book Next Block" (exact)
Failing any of these will break large swathes of the test suite silently.

CB-12 NOTE
----------
CB-12 has no standalone spec file. It's covered by CB-01's spec (the
new-client happy path naturally exercises T&Cs agreement). Coverage
Tracker shows it as ✅ with that note.

PB-03 NOTE
----------
PB-03 has no standalone spec file. It's covered by PB-10 (confirmed
booking on previous block grants priority — same assertion as PB-03's
eligible-client scenario). Coverage Tracker shows it as ✅ with that note.

PB-08 NOTE
----------
PB-08's Excel scenario originally referenced the Wednesday class. Monday
is the only fixture class where the priority gate renders (requires both
an active current block AND an upcoming block). PB-08 was implemented
against Monday using returning-two. Excel scenario wording updated in
Session 17 to match.

WORKFLOW DISCIPLINE — SESSION 16 LEARNING
-----------------------------------------
Recurring failure mode: Claude making unilateral mid-batch changes
instead of stopping to ask.

Hard rule going forward: any time the agreed batch scope changes for ANY
reason — stop, surface in two lines, wait for explicit sign-off before acting.

Documentation is locked until tests pass: do NOT update TEST-PLAN.md,
context.txt, or the Excel scenarios sheet during a batch in progress.

Self-trigger phrases — if Claude finds itself about to type any of these,
that's the cue to stop and ask: "I'll just...", "Easiest thing is to...",
"Let me also...", "While I'm at it...", "It makes sense to...",
"I'll defer/skip/replace this for now..."

SESSION 33 LEARNINGS — EMAIL NOTIFICATIONS SESSION 1, SUPABASE CLI, RESEND
--------------------------------------------------------------------------
- Resend does not offer a dedicated test API key type. All keys are live and
  deliver real emails. The test/prod distinction is handled by passing
  `isTest: true` in the Edge Function payload — the function redirects all
  recipients to `delivered@resend.dev` (Resend's silent sink).
- Supabase CLI requires Docker Desktop to bundle Edge Functions.
- `supabase login` may fail silently if macOS Keychain blocks the token.
  Workaround: export SUPABASE_ACCESS_TOKEN=your_token_here (session-scoped).
- Edge Function lives at `supabase/functions/send-email/index.ts`.
- The `send-email` Edge Function payload: `{ to, subject, html, isTest }`.

SESSION 34 LEARNINGS — EMAIL TRIGGER 1, NOEMAIL FLAG, TEST NOISE
----------------------------------------------------------------
- IS_NO_EMAIL flag: app-url.js APP_PATH includes ?noemail=1 automatically;
  sendBookingEmail() returns early if IS_NO_EMAIL is true.
- SE-12 uses APP_PATH_EMAIL (no noemail flag) so page.route() can intercept.
- buildReservedEmailHtml() uses table-based HTML for email client compatibility.

SESSION 35 LEARNINGS — EMAIL TRIGGER 2, EMAILPROMISE PATTERN
-------------------------------------------------------------
- confirmBookingAdmin() fires a "Confirming..." toast immediately (synchronous),
  then calls sendBookingEmail() async. Use emailPromise pattern — a Promise
  that resolves inside the page.route() callback — rather than waiting for
  the toast. SE-13 is the reference implementation.

SESSION 36 LEARNINGS — EMAIL TRIGGER 5, ARRAY INTERCEPT PATTERN
----------------------------------------------------------------
- When multiple Edge Function calls fire in the same booking flow, use
  `const capturedPayloads = []; ... capturedPayloads.push(body);` array
  pattern. Single variable is overwritten by last call.
- Dashboard URL in admin alert emails built dynamically from window.location.

SESSION 37 LEARNINGS — EMAIL TRIGGER 3, EMAIL WORDING REVIEW, BLW-09
---------------------------------------------------------------------
- Client cancellation email removed — client only gets email when Louise
  clicks Mark Refunded (or auto-send if refund = £0).
- Zero-refund auto-send: `if(refund===0&&cancelOpts.email)` in rfbConfirm().
- buildRefundClientEmailHtml serves dual purpose: zero-refund (red banner
  only) and Mark Refunded (red banner + green refund box).
- renderBlockWarnings() is now async — fires Supabase SELECT on cancellations
  to count pending refunds.
- GitHub Actions CI paths filter narrowed to specific subdirectories so
  doc-only pushes don't trigger a test run.

SESSION 38 LEARNINGS — CARD REDESIGN, PLAYWRIGHT SELECTOR HOOKS
----------------------------------------------------------------
- A major card visual redesign changed the HTML structure. 56 tests failed
  on first run because selectors were renamed or removed. Root causes:
  (1) .card-when-day contained c.name instead of c.day
  (2) .card-loc was hidden (display:none) — SD tests need it visible or readable
  (3) .cap-txt was removed — EC/CB tests assert on its text content
  (4) .badge.b-full was hidden — EC-01 asserts toBeVisible()
  (5) Book button labels changed to "Book Now" — booking-flow.js clicks by exact text
  (6) .priority-info class missing from locked/priority window divs — PB-01/02
- Rule: when redesigning card HTML, ALWAYS check the six selector hooks listed
  in CARD REDESIGN SELECTOR DISCIPLINE above before running tests.
- Cosmetic-only changes (colours, fonts, spacing, new elements) don't need
  new Playwright tests. Structural changes that rename or remove elements do.

SESSION 39 LEARNINGS — ADMIN DASHBOARD REDESIGN
------------------------------------------------
- The admin dashboard was completely restructured from a tab + below-tab
  layout to a sidebar with 8 distinct pages. The old #tab-bookings,
  #tab-classes, #tab-customers, #tab-cancellations elements no longer exist.
- 76 Playwright tests failed on first run due to stale selectors. All were
  fixed by updating admin-auth.js (loginAsAdmin, openClientsTab) and 40+
  spec files to use the new #dbnav-* and #dbpage-* selectors.
- Key lesson: when the dashboard navigation structure changes, admin-auth.js
  is the single highest-leverage fix — loginAsAdmin() is called by every
  admin spec. Fixing it unblocks the majority of failures immediately.
- #ctbody (classes table) is only visible when the Classes page is active.
  BLW specs that previously used `#ctbody tr` as a dashboard readiness check
  were updated to use `#btbody tr` (bookings table, always visible after login).
- The global CSS `nav { display:flex }` rule conflicts with any `<nav>` element
  used as a sidebar. Always use `<div>` for sidebar navigation containers.
- The dashboard redesign was tagged in git as `v-pre-dashboard-redesign`
  before work began, providing a clean rollback point.

SESSION 40 — PAYMENT MODE FEATURE SCOPED
-----------------------------------------
- Payment mode feature flag fully scoped. Spec saved as PAYMENT-MODE-SPEC.md
  at repo root. 6 sessions planned (PM-1 to PM-6).
- Stripe Checkout chosen over Stripe Elements: Stripe hosts the payment page,
  handles card / Apple Pay / Google Pay, and takes on PCI compliance.
  Much simpler to build and test than embedded Elements.
- Feature flag approach: single `payment_mode` setting in the settings table
  (`bank_transfer` or `stripe`). No parallel codebases. Louise toggles from
  the admin Settings page.
- Stripe secret key and webhook secret must NEVER go in the settings table or
  index.html — Edge Function environment variables only.
- Publishable key (safe, public) stored in settings table under `stripe_publishable_key`.
- Webhook is the authoritative payment confirmation signal — the success
  redirect URL alone must never be the sole trigger for confirming a booking.
- All existing bank transfer email triggers (1–6) remain intact in bank
  transfer mode. Stripe mode suppresses triggers 1 and 5, moves trigger 2
  to the webhook handler, and adds new trigger 5S (Louise payment alert).
- PM-1 and PM-2 require no Stripe account. PM-3 onwards requires Stripe
  test keys — Mark to create Stripe account before that session.
- BACKLOG.md needs updating at the start of PM-1 to add this as a Tier 1 item.
- Mark's preference for PM sessions: explain aim and objectives in plain
  English at the start of every session; keep jargon low; one step at a
  time with sign-off at each step.

SESSION 41 LEARNINGS — PM-2 BOOKING MODAL BRANCH, ST-07/ST-08
--------------------------------------------------------------
- PM-2 branches Step 4 of the booking modal on PAYMENT_MODE. HTML elements:
  #step4-bank-section (visible in bank_transfer), #step4-stripe-section
  (visible in stripe), #reserve-btn (bank transfer footer), #stripe-pay-btn
  (Stripe footer, background var(--charcoal)). goStep3() and onTcsChange()
  both branch on PAYMENT_MODE. initiateStripeCheckout() is a console-only
  placeholder until PM-3.
- The anon sb client CANNOT update the settings table — RLS allows INSERT
  for anon but UPDATE is auth-only. Specs that need to write payment_mode
  must use direct pg (Pool from TEST_SUPABASE_DB_URL), same pattern as
  admin-db.js. Silently failing upserts are the failure mode — no error
  thrown, the DB value simply doesn't change.
- returning-one already has a confirmed booking on mon-current, so using
  that card in the booking modal triggers the duplicate-detection path
  (showAlreadyBookedView) instead of advancing to Step 4. Use fri-upcoming
  for returning-client Step 4 specs — returning-one has no booking there.
- ST specs that set payment_mode to stripe must always restore bank_transfer
  in afterEach, even if the test fails mid-way, to prevent mode bleed into
  other specs in the suite.

SESSION 42 LEARNINGS — PM-3 STRIPE CHECKOUT, PENDING BOOKINGS
--------------------------------------------------------------
- Stripe account: Mark's account, GBP, test mode. Secret key stored as
  SUPABASE_SECRET_KEY Edge Function secret on both prod and test projects.
  Publishable key stored in settings table under stripe_publishable_key.
- stripe-checkout Edge Function (v2, deployed via MCP): inserts a
  pending_bookings row using service role, creates Stripe Checkout Session
  with pending_booking_id as metadata, returns the Stripe URL.
  CLI deployment failed silently on first attempt — use MCP deploy_edge_function
  as the reliable fallback when CLI deploys don't take.
- Do NOT save a real booking before Stripe redirects — create a pending_bookings
  row instead. The webhook (PM-4) creates the real booking after payment confirmed.
- The "Proceed to Payment" button (id="stripe-pay-btn") calls confirmBooking()
  (same as reserve-btn), NOT initiateStripeCheckout() directly. confirmBooking()
  branches on PAYMENT_MODE — bank transfer path unchanged, Stripe path writes
  to pending_bookings via Edge Function and redirects.
- Payment success screen: full overlay (#stripe-success-overlay) shown on
  ?payment=success redirect — not a toast. Dismissed via "Back to Schedule" button.
  Currently shows generic "Confirmed / Check your email" rows. PM-4 webhook will
  populate real booking details once confirmed.
- Stripe takes ~1.5% + 20p per transaction. Louise receives the net amount.
- anon role cannot SELECT from pending_bookings — service role (Edge Function)
  handles all reads and deletes on that table.
- pending_bookings rows expire_at = NOW() + 2 hours. No automated cleanup yet —
  add a scheduled function post-launch or clean manually via SQL editor.
- Running index.html as file:// (not via http server) silently breaks all
  cross-origin Edge Function calls. Always use python3 -m http.server 8000.

SESSION 43 LEARNINGS — PM-4 STRIPE WEBHOOK, EDGE FUNCTION PARITY GAP
----------------------------------------------------------------------
- EDGE FUNCTION PARITY GAP DISCOVERED: stripe-checkout v2 (Session 42) had only
  been deployed to PRODUCTION, not test — test was still on v1 (old booking_id
  signature). The session-start drift check covers DB schema/fixture data only,
  NOT Edge Function code — there's no automated parity check between prod/test
  function deployments. Redeployed v2 (then v3) to test to match. ACTION ITEM:
  add an Edge Function parity check to the session-start routine or schema-check
  tooling (see TO DO / AUDIT ITEMS).
- stripe-checkout bumped to v3: added app_base_url to the metadata sent to
  Stripe (front end passes window.location.origin+pathname). stripe-webhook
  reads this back from session.metadata to build the admin dashboard link,
  since Edge Functions have no window.location.
- stripe-webhook (new, Deno-native, no Stripe SDK): verifies Stripe signature
  via HMAC-SHA256 over "{timestamp}.{rawBody}" using STRIPE_WEBHOOK_SECRET.
  verify_jwt: false (Stripe can't send a Supabase JWT — the signature check
  IS the auth gate). Listens for checkout.session.completed only.
- On success: upsert_customer → book_if_available → bookings.update(status=
  'confirmed', stripe_payment_intent_id, stripe_checkout_session_id) → PAR-Q
  insert if new client → send client confirmation (trigger 2) + admin alert
  (trigger 5S) emails → delete pending_bookings row.
- SCHEMA GAP FOUND DURING TESTING: bookings table was missing
  stripe_payment_intent_id/stripe_checkout_session_id entirely on BOTH prod
  and test (context.txt previously claimed these existed from "migration 12" —
  this was inaccurate, no such migration was ever applied). First webhook test
  ran upsert_customer + book_if_available successfully (booking created,
  pending_bookings row cleaned up, BOTH emails sent correctly) but the final
  status='confirmed' update failed silently (Supabase JS .update() without
  .select() swallows errors — known gotcha). Booking was left at status=
  'reserved'. Fixed via ALTER TABLE adding both columns (nullable text) to
  bookings on test AND production. Re-tested successfully — booking landed as
  confirmed with both Stripe IDs populated.
- NEW: admin failure-alert email added to stripe-webhook for the case where
  payment succeeds but book_if_available fails (CLASS_FULL or ALREADY_BOOKED —
  e.g. class fills in the seconds before webhook fires). Sends a red-banner
  email to admin_email with client contact details, amount paid, and the
  pending_booking_id as a reference, flagging manual refund needed. The
  pending_bookings row is deliberately left in place (not deleted) in this
  case for manual review.
- Two Stripe test payments completed successfully end-to-end (bookings 4579,
  4580 on test project — class_id 3 & 4, £60 and £40 pro-rata). Both emails
  (client confirmation + admin alert) delivered to delivered@resend.dev.
  These bookings + the "Mark Jones" / mjones970@live.co.uk test customer still
  need clearing via npm run seed before next Playwright run (reseed was run
  once mid-session to clear an earlier failed attempt, but NOT after these
  two successful ones).
- stripe-webhook deployed to TEST ONLY. Production has no webhook function and
  no Stripe webhook endpoint configured yet — PM-5 next session.
- No migration file exists yet for: pending_bookings table (Session 42),
  stripe_payment_intent_id/stripe_checkout_session_id columns (Session 43),
  or the stripe-checkout/stripe-webhook Edge Functions. ACTION ITEM: create
  migration files for these so file-on-disk matches DB-state (Session 16 rule).

SESSION 44 LEARNINGS — PM-5 PRODUCTION WEBHOOK, EMAIL PILLS, PAYMENT_MODE RESEED BUG
---------------------------------------------------------------------------------------
- PM-5 complete: stripe-webhook deployed to production (same code as test v2/v3,
  no environment-specific values — webhook reads everything from env vars and
  request metadata). Production webhook endpoint created in Stripe
  (lg-pilates-prod-webhook), STRIPE_WEBHOOK_SECRET set on production Edge
  Function secrets. End-to-end test successful: booking 227 confirmed with
  stripe_payment_intent_id + stripe_checkout_session_id populated.
- buildConfirmedEmailHtml (client confirmation email, trigger 2) updated to
  show a "Sessions" row of date pills (past dates greyed, matching the admin
  alert email's style) instead of a single "Block dates" range row. Applied
  to index.html AND both stripe-webhook deployments (test + prod, now v3) —
  this email is built independently in three places and all three needed
  updating to stay consistent.
- BUG FOUND AND FIXED: 20 Playwright specs failed with #reserve-btn stuck
  disabled/hidden after ticking T&Cs at Step 3/4. Root cause: settings.payment_mode
  on the test DB was left as 'stripe' from earlier PM testing. Migration 09
  (the reseed) never touches settings, so a reseed does NOT reset payment_mode —
  it carries over from whatever state the DB was already in. With payment_mode
  stuck on 'stripe', onTcsChange() enables #stripe-pay-btn (display:none) instead
  of #reserve-btn, which every bank-transfer-flow spec waits on. The failing run's
  later ST-suite afterEach reset payment_mode back to bank_transfer, which is why
  a direct DB query after the run showed the "correct" value — masking the cause
  until the failure was reproduced via screenshot.
- FIX: new migration 12 (12_reseed_resets_payment_mode.sql) upserts
  payment_mode='bank_transfer' on every reseed, leaving stripe_publishable_key
  untouched (so it doesn't need re-entering via the admin UI). Added to
  seed.js's MIGRATION_FILES array. Applied directly to test DB this session;
  re-run of npm test passed 178/178.
- RULE: any setting that PM-session testing temporarily changes (payment_mode,
  and potentially others in future) should have its "safe default" enforced by
  the reseed migration, not just by individual specs' afterEach — afterEach
  doesn't run on every failure mode (e.g. process crash), and reseed is the
  one guaranteed reset point.
- Stripe's "Delete test data" feature (test-mode sandbox) only clears Stripe-side
  transaction records (PaymentIntents, Checkout Sessions, Charges etc.) — it does
  NOT affect webhook endpoints/secrets, Edge Functions, or Supabase data
  (pending_bookings, bookings). Safe to use for Stripe-side housekeeping; Supabase
  cleanup is separate.
- Outstanding from Session 44, resolved in Session 45: the 4 pending_bookings
  rows on PRODUCTION were cleared, and production payment_mode was set to
  'stripe' (deliberately — system not yet public, see TO DO item 4).

SESSION 45 LEARNINGS — PM-6 PLAYWRIGHT COMPLETE (ST-17 TO ST-26)
-------------------------------------------------------------------
- ST numbering collision: TEST-PLAN.md's Coverage Tracker already had IDs
  ST-09 to ST-18 reserved from early PM planning (ST-16 = the existing
  badge spec, ✅; ST-09/10/11/12/13/14/15/17/18 = ⬜ placeholders describing
  a pre-pending_bookings design). New specs were filed on disk as st-17 to
  st-26 to avoid the st-16 filename clash. The OLD ST-17/ST-18 placeholder
  rows were overwritten with the new specs' descriptions; ST-09 to ST-15
  remain as open placeholders (ST-10/ST-11 describe the superseded
  pre-pending_bookings design and are effectively covered by ST-17/18/19;
  ST-12/ST-15/ST-09 remain genuinely open if wanted in future).
  RULE: before assigning new ST-NN numbers, check TEST-PLAN.md's Coverage
  Tracker for already-reserved IDs, not just existing filenames.
- pg bigint columns (class_id, block_id, customer_id, booking/parq ids) come
  back from node-pg as STRINGS, while the same values via supabase-js/RPC
  JSON responses come back as NUMBERS. Always wrap in Number() when comparing
  across the two (bit ST-17 hit this first: pending_bookings.class_id/block_id
  needed Number() against the fixture's numeric values).
- DATE columns (e.g. parq.sign_date) come back from node-pg as JS Date objects
  set to LOCAL midnight. Using .toISOString() shifts them back a day during
  BST — same root cause as the existing toLocalISO() note for writes, but
  this is the READ-side equivalent. Extract via getFullYear()/getMonth()/
  getDate(), not toISOString().
- stripe-webhook (test project, v3) has NO local source file — retrieved live
  via Supabase MCP get_edge_function for this session. Contains
  buildConfirmedEmailHtml, buildAdminAlertEmailHtml, and
  buildPaymentFailedAdminEmailHtml (the last one Mark hadn't seen rendered
  before — only fires on the rare CLASS_FULL/ALREADY_BOOKED-after-payment
  race condition).
- NEW SHARED TEST HELPER PATTERN — "template-fidelity" tests: ST-21/ST-22
  test email HTML by calling COPIES of buildConfirmedEmailHtml/
  buildAdminAlertEmailHtml in tests/helpers/email-templates.js, because
  webhook-to-send-email is server-to-server and Playwright's page.route()
  cannot intercept it (unlike SE-13's browser-triggered emails). This is now
  a 4th copy of these functions (index.html, test webhook, prod webhook,
  test helper). DRIFT RISK: if the email template changes again, this helper
  must be updated too or ST-21/22 will pass against stale logic. Flagged in
  BACKLOG.md as a candidate for consolidation into a shared module.
- New tests/helpers/stripe-webhook.js: signs and POSTs fabricated
  checkout.session.completed events directly to stripe-webhook (HMAC-SHA256,
  same scheme Stripe uses). Requires TEST_STRIPE_WEBHOOK_SECRET in .env.test
  (the test project's stripe-webhook STRIPE_WEBHOOK_SECRET value — added this
  session, not previously in .env.test or .env.test.example).
- admin-db.js gained 9 new helpers this session: setPaymentMode,
  getPendingBookingByEmail/ById, deletePendingBookingByEmail/ById,
  insertPendingBooking, countBookingsForCustomerOnBlock, getBookingById,
  getParqByCustomerId (existing, reused).
- KNOWN GAP (added to BACKLOG.md): when a Stripe payment succeeds but
  book_if_available fails (CLASS_FULL/ALREADY_BOOKED), the CLIENT receives
  no email at all — only Louise gets the failure alert. Combined with the
  front-end success overlay showing "Booking confirmed — check your email"
  immediately on the Stripe redirect (before the webhook even runs), a
  client in this edge case would believe their booking succeeded with no
  indication otherwise. Needs a proper review of the success-screen timing,
  not just an added email.
- TEST-PLAN.md housekeeping: the SE/ST split was previously inconsistent —
  st-07/st-08 were physically listed under the "Settings & Export" spec file
  list (making its header wrong at 20/20 vs actual 21/22) while the "Stripe"
  header claimed 9/9 but only listed 7. Moved st-07/st-08 into the Stripe
  list (SE now correctly 19/20, ST was 9/9 before PM-6 additions). The
  printed Tests Total was also stale by 2 (179 vs row-sum 181) — both fixed
  as part of this session's update. The Spec files Total has a small
  (~1-3 file) residual gap vs `ls tests/*.spec.js` on disk that predates this
  session and wasn't chased further (low value).

============================================================
