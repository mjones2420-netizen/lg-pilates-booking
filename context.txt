============================================================
LG PILATES BOOKING SYSTEM — CONTEXT FILE
Last updated: 16 May 2026
============================================================

ABOUT THIS FILE
---------------
This is a working context file, NOT a diary. It exists to give Claude
fast, durable context at the start of any session.

Rules to keep it useful:
- Capture WHAT IS TRUE NOW, not the narrative of how we got here.
- Session summaries belong in commit messages and chat history, not here.
- Before adding a "Session N: ..." note, ask: is this a durable gotcha
  that would help a future session, or is it just history? If history,
  leave it out.
- If a fact is already covered in a structured section above, don't
  re-state it as a key decision below.

============================================================
1. PROJECT OVERVIEW
============================================================

A complete Pilates class booking system for LG Pilates (Louise George).
Classes run in Baildon and Guiseley.
Single HTML file (index.html) deployed to GitHub Pages.
Backend: Supabase (Postgres database).
Mark is the non-developer owner. Claude is the primary technical collaborator.

LIVE URL:    https://mjones2420-netizen.github.io/lg-pilates-booking/
GITHUB:      https://github.com/mjones2420-netizen/lg-pilates-booking
DEMOS:       https://mjones2420-netizen.github.io/lg-pilates-booking/demos/
             (11 standalone interactive HTML demo files; several outdated
             since per-class priority + pro-rata + 4-step flow — see TO DO)

index.html is uploaded manually by Mark at the start of each session.
The GitHub connector is no longer used.

============================================================
2. SUPABASE PROJECTS
============================================================

PRODUCTION
----------
URL:        https://mrlooyixnlxzcfmvnqme.supabase.co
Project ID: mrlooyixnlxzcfmvnqme
Anon Key:   eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6Im1ybG9veWl4bmx4emNmbXZucW1lIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NzI5MTM2MDAsImV4cCI6MjA4ODQ4OTYwMH0.k1LbLenXS29NosaayGnQn3SR2G1xXVDBICeH7w7bD_c

TEST
----
URL:        https://ngzfhamjuviwfwuncrjo.supabase.co
Project ID: ngzfhamjuviwfwuncrjo
Anon Key:   eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6Im5nemZoYW1qdXZpd2Z3dW5jcmpvIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NzY1OTM1ODksImV4cCI6MjA5MjE2OTU4OX0.okVYL1VLSDd3I7nD5VPuUCOZmY_G7mzC4o3Y3lx16Pg

Schema is a byte-for-byte mirror of production. Verify with:
    npm run schema-check
Both should return fingerprint dad311c8a152bc0dbec1cb3fa0c584d5, 248 objects.

Test admin user:
  Email:    admin@lg-pilates-test.local
  Password: stored in tests-playwright/.env.test as TEST_ADMIN_PASSWORD
  IMPORTANT: Test admin users MUST be created via the Supabase dashboard
  "Add user" button. Raw SQL inserts into auth.users do NOT populate
  auth.identities, which Supabase Auth requires for password login.

============================================================
3. ENV SWITCH (?env=test)
============================================================

index.html supports a URL query parameter ?env=test that switches the
Supabase client (data and auth) from production to the test project.

- No query param  → production project, no banner
- ?env=test       → test project, red banner injected at DOMContentLoaded

Code lives at the top of the script section in index.html (around line 800).
Banner: fixed-top red div, id="test-mode-banner", class="on" — this is
the assertion target Playwright uses in every CB spec's beforeEach as a
second line of defence against tests hitting production.

Production behaviour is byte-identical to before the switch existed when
the URL has no ?env=test parameter.

============================================================
4. DATABASE SCHEMA
============================================================

TABLES
------
classes                  — permanent class slots (id, name, level, day, time, end_time, venue, loc)
blocks                   — each 6-week block run (id, class_id, start_date, end_date, weeks, dates[], price, cap, booked, visible, status)
bookings                 — client bookings (id, class_id, block_id, customer_id, status, amount_due, refund_amount, created_at)
customers                — client records (id, first_name, last_name, email, phone, customer_type, priority, created_at)
                           NOTE: customers.priority column is retained in DB but no longer used by the app.
                           Per-class manual priority is now managed via customer_class_priority table.
parq                     — health forms (id, booking_id, customer_id, age, emergency_name, emergency_relationship, emergency_phone, q1_heart...q12_other_reasons, yes_details, additional_notes, print_name, sign_date)
                           NOTE: sign_date is DATE type. Front-end stores ISO (YYYY-MM-DD) in a data-iso
                           attribute on the display element and sends that direct to the DB.
                           Admin PAR-Q view uses formatDateDisplay() to render "18 Apr 2026".
settings                 — key/value store for bank details (bank_name, bank_sort_code, bank_account_no)
waitlist                 — placeholder for future waitlist feature (id, class_id, customer_id, created_at)
                           RLS enabled. Empty — not yet connected to booking flow.
cancellations            — audit trail of all client removals from blocks
                           Columns: id, customer_id, class_id, block_id, first_name, last_name, email,
                           class_name, venue, block_start_date, block_end_date, sessions_attended,
                           sessions_remaining, price_per_session, refund_amount, refunded (BOOLEAN),
                           refunded_at (TIMESTAMPTZ), cancelled_at (TIMESTAMPTZ)
                           NOTE: first_name/last_name/email/class_name/venue are denormalised so records
                           survive even if the customer or class is later deleted.
customer_class_priority  — per-class manual priority grants
                           Columns: id (BIGSERIAL), customer_id (BIGINT FK → customers),
                           class_id (BIGINT FK → classes), granted_at (TIMESTAMPTZ)
                           UNIQUE constraint on (customer_id, class_id)
                           ON DELETE CASCADE on both FKs
                           A row present = that customer has manual priority for that class.
                           This table replaces the global customers.priority boolean.

NOTE: parq table uses emergency_name, emergency_relationship, emergency_phone (NOT emergency_contact — dropped)
NOTE: parq.booking_id FK is ON DELETE CASCADE — PAR-Qs auto-delete when their booking is deleted.
NOTE: bookings.block_id FK is ON DELETE CASCADE — prevents orphan bookings.
NOTE: App-level deletion code still clears child rows explicitly as belt-and-braces safety net.
NOTE: When bulk-deleting all client data, deletion order must be:
parq → cancellations → bookings → customer_class_priority → customers

CHECK CONSTRAINTS
-----------------
bookings:
  - bookings_status_check: status IN ('reserved','confirmed','cancelled','refund-pending','refunded','waitlist')

blocks:
  - blocks_status_check:    status IN ('upcoming','active','completed','cancelled')
  - blocks_price_nonneg:    price >= 0
  - blocks_cap_positive:    cap >= 1
  - blocks_weeks_positive:  weeks >= 1

customers:
  - customers_type_check:      customer_type IN ('new','returning','vip') OR customer_type IS NULL
  - customers_first_name_len:  char_length(first_name) <= 100
  - customers_last_name_len:   char_length(last_name)  <= 100
  - customers_email_len:       char_length(email)      <= 200
  - customers_phone_len:       char_length(phone)      <= 40

parq:
  - parq_yes_details_len:      char_length(yes_details)            <= 4000
  - parq_additional_notes_len: char_length(additional_notes)       <= 4000
  - parq_emergency_name_len:   char_length(emergency_name)         <= 100
  - parq_emergency_rel_len:    char_length(emergency_relationship) <= 50
  - parq_emergency_phone_len:  char_length(emergency_phone)        <= 40
  - parq_print_name_len:       char_length(print_name)             <= 100

IMPORTANT: customer_type must be lowercase 'new' or 'returning' — the
customers_type_check constraint rejects capitalised variants.

NOT NULL CONSTRAINTS
--------------------
customers:    email NOT NULL
bookings:     customer_id, class_id, block_id NOT NULL
blocks:       class_id, start_date, end_date NOT NULL
classes:      name, day, venue NOT NULL

NOT NULL violation = Postgres error code 23502; existing try/catch blocks
surface a generic toast ("Error saving class." / "Error adding block.").

PARTIAL UNIQUE INDEX
--------------------
bookings_unique_active_per_block:
  CREATE UNIQUE INDEX ON bookings (customer_id, block_id) WHERE status != 'cancelled'

Prevents duplicate non-cancelled bookings by same customer on same block, at DB level.
Re-booking after cancellation still works — cancelled rows excluded.
book_if_available RPC catches unique_violation and re-raises as ALREADY_BOOKED
so the front-end handles it identically.

SECURITY DEFINER FUNCTIONS
--------------------------
These bypass RLS and are called from the JS client instead of direct table access:

- lookup_customer(p_email) — returns customer record by email
- upsert_customer(p_first_name, p_last_name, p_email, p_phone, p_customer_type)
    inserts or updates customer, returns id
- book_if_available(p_block_id, p_class_id, p_customer_id, p_amount_due)
    atomic capacity check + booking insert, returns booking id
- check_priority_access(p_email, p_block_id) → BOOLEAN
    checks customer_class_priority (per-class manual) then previous-block
    confirmed booking
- has_active_booking_on_block(p_customer_id, p_block_id) → BOOLEAN
    true if customer has a non-cancelled booking on this block.
    Used by the booking modal step-1 handler.
- get_schema_fingerprint() → (fingerprint TEXT, total_objects BIGINT)
    Read-only deterministic hash of the public schema's structure (columns,
    constraints, indexes, RLS policies, functions, triggers, grants).
    Self-excluded from its own fingerprint. Used by the schema-check helper.

All functions granted EXECUTE to anon and authenticated roles.

DATABASE TRIGGERS
-----------------
trg_sync_block_booked_count — fires AFTER INSERT OR DELETE on bookings.
Calls sync_block_booked_count() which recalculates blocks.booked from actual
bookings (excluding cancelled). SECURITY DEFINER, SET search_path = public.

IMPORTANT: This trigger fires on app-level inserts/deletes only. If bookings
are bulk-modified via raw SQL, the booked count will NOT update automatically.
After raw SQL changes to bookings, run this resync:

    UPDATE blocks b
    SET booked = (
      SELECT COUNT(*) FROM bookings
      WHERE block_id = b.id AND status != 'cancelled'
    );

RLS POLICIES
------------
RLS enabled on all tables.

classes:                 SELECT anon+auth | INS/UPD/DEL auth only
blocks:                  SELECT anon+auth | INS/UPD/DEL auth only
bookings:                INSERT anon+auth (routes via SECURITY DEFINER) | SELECT/UPD/DEL auth only
customers:               INSERT anon+auth (routes via SECURITY DEFINER) | SELECT/UPD/DEL auth only
parq:                    INSERT anon+auth | SELECT/DEL auth only (no UPDATE — write-once)
settings:                SELECT anon+auth | INS/UPD auth only
waitlist:                ALL auth only
cancellations:           ALL auth only
customer_class_priority: ALL auth only

NOTE: customers and bookings have no anon SELECT policy — intentional. All
public reads of customer/booking data must go via SECURITY DEFINER functions.

ANON ROLE GRANTS
----------------
Table grants to anon:
  classes   — SELECT only
  blocks    — SELECT only
  settings  — SELECT only
  parq      — INSERT only (PAR-Q save during booking)

Tables with NO anon grants at all:
  bookings, customers, cancellations, waitlist, customer_class_priority

Function EXECUTE grants to anon:
  book_if_available, check_priority_access, has_active_booking_on_block,
  lookup_customer, upsert_customer, get_schema_fingerprint

The `authenticated` role retains ALL on every table (admin dashboard unaffected).

============================================================
5. FRONT-END
============================================================

CSS VARIABLES
-------------
--cream: #eef5f5 | --warm: #f7fafa | --sage: #3a8a8a | --sage-dk: #2a6b6b
--sage-lt: #b8d8d8 | --clay: #e07b4a | --charcoal: #1a2e2e | --mid: #4a6060
--light: #8aabab | --border: #cde0e0 | --green: #3a8a6a | --red: #c04040

STYLING GUIDE
-------------
Fonts:
  - Headings, logo, large numbers: Raleway (300, 400, 500, 600)
  - Body text, labels, buttons: Open Sans (300, 400, 500)

Border radius:
  - Standard: 4px (sharp — cards, modals, inputs, buttons)
  - Pills/badges only: 100px
  - Circular avatars/icons: 50%

Spacing:
  - Page padding: 40px horizontal desktop / 18px mobile
  - Card padding: 26px
  - Modal body padding: 20px 28px

Buttons:
  Primary:        var(--charcoal) bg, white, border:none, Open Sans 12-14px,
                  letter-spacing 0.1em, text-transform uppercase, border-radius 2px,
                  padding 13-16px
  Secondary/back: transparent bg, 1px solid var(--border), var(--mid)
  Sage variant:   var(--sage-dk) bg (admin export buttons)

Booking modal:
  - .modal: max-width 500px, max-height 92vh, overflow-y auto (single scroll container)
  - .mhead: position sticky, top 0, z-index 10, background var(--warm) — sticky header
  - .mbody: padding 20px 28px — plain wrapper, NOT the scroll container
  - Step pips: 4 for new clients / 2 for returning clients

  CRITICAL: Do NOT convert .modal to a flex container — this breaks mobile scroll.

SHARED JS HELPERS
-----------------
- calcBlockDates(startDateStr, weeks) — builds dates array in "D MMM" format
- sanitise(str) — HTML-escapes strings for safe DOM insertion
- formatDateDisplay(isoStr) — formats YYYY-MM-DD → "D MMM YYYY" for display
- renderDatePills(dates) — sanitised date pill HTML
- populateBankDetails(name, sort, acc) — updates all bank detail DOM elements
- validateBlockOverlap(classId, startStr, weeks, excludeBlockId) — checks overlaps
- PARQ_LABELS / PARQ_KEYS — 12-element shared constants
- calcProrata(blk) — sessions remaining, pro-rata price, full price
- showAlreadyBookedView() — renders the duplicate-booking early-detection view

COMPLETED FEATURES
------------------
Client-facing:
- Class cards with day/time prominent, venue/level, current block panel
- Date pills: past/today/upcoming awareness
- Pro-rata pricing
- Next block collapsible toggle with priority/standard/locked icons
- Booking modal — 4 steps new / 2 steps returning
- Already-booked early detection view (returning client step 1)
- Priority booking system — priority 8-14 days before block start, standard 0-7 days

Admin dashboard (password protected via Supabase Auth):
- All Bookings tab with Missing PAR-Q banner
- By Class tab — accordion
- Clients tab with per-class priority management
- Cancellations tab with audit trail and CSV export
- Upcoming Classes section — add/edit classes and blocks
- Settings — bank details
- Backup & Export — CSV with formula-injection protection
- Block warnings banner — red/yellow advisories
- Block overlap prevention
- Test-mode env switch (URL query param ?env=test)

CLASSES (PRODUCTION — IDs 28–32)
---------------------------------
28: Mixed Ability, Mon 9:45am–10:30am, Baildon Moravian Church, Baildon
29: Intermediate,  Mon 6:30pm–7:15pm,  St Johns Parish Church,  Baildon
30: Mixed Ability, Tue 9:45am–10:30am, Starlight Academy,       Guiseley
31: Beginner,      Wed 7:00pm–7:45pm,  Baildon Moravian Church, Baildon
32: Beginner,      Fri 10:30am–11:15am, Potting Shed,           Guiseley

BOOKING WINDOW LOGIC
--------------------
- >14 days before block start: LOCKED — timeline shown
- 8-14 days before:            PRIORITY WINDOW — priority clients can book
- 0-7 days before:             STANDARD WINDOW — all can book

============================================================
6. PLAYWRIGHT TEST SUITE
============================================================

PURPOSE
-------
Automated regression pack for the booking system. Runs against the isolated
lg-pilates-test Supabase project so real client data is never touched.

REPO LAYOUT
-----------
tests-playwright/
├── package.json                 # scripts: test, test:headed, test:ui, test:debug, report, seed, schema-check
├── playwright.config.js         # always-on trace/screenshot/video
├── .env.test.example            # template, committed
├── .env.test                    # secrets, gitignored — TEST_APP_URL, TEST_SUPABASE_DB_URL,
│                                #   TEST_ADMIN_PASSWORD, PROD_SUPABASE_URL, PROD_SUPABASE_ANON_KEY
├── README.md
├── TEST-PLAN.md                 # plain-English description of every test (updated each batch)
├── migrations/                  # SQL migrations applied to test DB
│   ├── 01_tables.sql
│   ├── 02_foreign_keys.sql
│   ├── 03_indexes.sql
│   ├── 04_functions.sql
│   ├── 05_trigger.sql
│   ├── 06_rls_and_grants.sql
│   ├── 07_seed_data.sql
│   ├── 08_seed_cb31_active_booking.sql    # obsolete — superseded by 09; do not re-run
│   ├── 09_reseed_with_dynamic_dates.sql   # live migration — idempotent, dynamic dates
│   ├── 10_add_schema_fingerprint_function.sql  # applied to BOTH prod and test
│   ├── 11_add_locked_window_class.sql     # Session 16 — Thu class for PB-01 locked window
│   └── README.md
├── scripts/
│   ├── seed.js                  # npm run seed wrapper (safety checks + verification)
│   ├── schema-check.js          # compares prod vs test schema fingerprint
│   └── SCHEMA-CHECK-README.md
└── tests/
    ├── safety-check.js
    ├── helpers/
    │   ├── supabase.js          # shared anon client (exports `sb`)
    │   ├── app-url.js           # APP_PATH helper
    │   ├── booking-flow.js      # CB test helpers (incl. agreeAndReserve)
    │   ├── fixture-lookup.js    # role-based block lookup (getBlockByRole)
    │   ├── admin-auth.js        # admin login + per-class panel UI helpers (Session 17)
    │   └── admin-db.js          # direct pg helper for admin-only tables (Session 17)
    │                            # Exports: grantManualPriority, removeManualPriority,
    │                            #          hasManualPriority, setBookingStatus (S18),
    │                            #          resyncBlockBookedCount (S18),
    │                            #          deleteBookingsForCustomerOnBlock (S18),
    │                            #          deleteCustomerCascade (S18),
    │                            #          getParqByCustomerId (S19, used by CB-33
    │                            #          after Session 20 .select() fix)
    ├── smoke-01-anon-reads.spec.js
    ├── smoke-02-anon-rpcs.spec.js
    ├── smoke-03-rls-enforcement.spec.js
    ├── smoke-04-ui-page-loads.spec.js
    ├── cb-*.spec.js             # 23 CB spec files covering 34 of 34 scenarios
    ├── pb-*.spec.js             # 14 PB spec files covering 10 Excel scenarios + 5 PB-X gap-analysis tests
    ├── sd-*.spec.js             # 6 SD spec files covering 6 of 6 scenarios
    ├── acl-*.spec.js            # 2 ACL spec files covering 2 of 2 genuine scenarios
    ├── bw-*.spec.js             # 3 BW spec files covering 3 of 3 genuine scenarios
    └── sec-*.spec.js            # 3 SEC spec files covering 3 of 3 genuine scenarios

CURRENT TEST COUNT: 81 tests (all passing — Session 23, Batch 10 complete)
  Smoke: 14 tests (anon reads, RPCs, RLS enforcement, UI loads)
  CB:    34 tests covering 34 of 34 CB scenarios (CB suite complete)
  PB:    16 tests — 9 cover 10 of 10 PB Excel scenarios + 7 gap-analysis tests
         (PB-X1 has 3 sub-tests, PB-X2 through PB-X5 have 1 each)
  SD:    6 tests covering 6 of 6 SD scenarios (SD suite complete)
  ACL:   2 tests covering 2 of 2 genuine ACL scenarios (ACL suite complete)
  BW:    3 tests covering 3 of 3 genuine BW scenarios (BW suite complete)
  SEC:   6 tests in 3 spec files covering 3 of 3 genuine SEC scenarios
         (SEC-02 has 2 sub-tests, SEC-06 has 3 sub-tests, SEC-07 has 1.
          SEC-01/03/04/05 are red-filled in Excel as duplicates of other specs.)

CB-12 is covered by CB-01 (the new-client happy path includes T&Cs agreement).
The Coverage Tracker shows it as ✅ for that reason — no separate spec file exists.
CB-16b covers Step 3 → Step 4 advance — a transition not present in the old
3-step Excel scenarios but real in the current 4-step booking flow.
PB-03 is covered by PB-10 (positive-path priority granted via confirmed booking
on previous block — same assertion as PB-03's eligible-client scenario).

Coverage Tracker is in tests-playwright/TEST-PLAN.md at the top — that file
is the authoritative view of automation progress for all CB and PB scenarios.

THE SAFETY CHECK (non-negotiable)
----------------------------------
tests/safety-check.js is loaded by playwright.config.js BEFORE any test runs.
It throws and crashes the run if:
  1. TEST_SUPABASE_PROJECT_ID is missing
  2. TEST_SUPABASE_PROJECT_ID equals production (mrlooyixnlxzcfmvnqme)
  3. TEST_SUPABASE_URL contains the production project reference
  4. TEST_SUPABASE_ANON_KEY is missing
  5. The anon key's JWT 'ref' claim is the production project ID

LIMITATION: This safety check only guards the test helper's Supabase client.
It does NOT guard the browser page's own Supabase client. The TEST MODE banner
assertion in CB test beforeEach() hooks is the second line of defence.

TEST FIXTURE — DYNAMIC DATES
----------------------------
Migration 09 dynamically computes all block dates from CURRENT_DATE at the
moment the migration runs, so the fixture always stays correctly positioned
relative to "today" — priority window is always priority, standard is always
standard, etc.

Migration 09 is idempotent and re-runnable. Running it again wipes the
existing fixture and rebuilds it fresh. Migration 11 (added Session 16)
adds the Thursday class for PB-01 — also idempotent. `npm run seed` now
applies BOTH 09 and 11 in sequence inside a single transaction.

CURRENT TEST FIXTURE — 11 blocks across 4 classes:

  Mon Mixed (class_id=1):
    - Mon past         (completed, priority-source)
    - Mon current      (active, CB-31 duplicate booking)
    - Mon upcoming     (priority window, ~+8 to +13 days out, PB-09/PB-10 target)
    - Mon full         (upcoming, ~+50 days out, cap=2, FULL)

  Wed Beginner (class_id=2):
    - Wed past         (completed, priority-source)
    - Wed upcoming     (priority window, ~+8 days out)

  Thu Mixed (class_id=4) [Migration 11]:
    - Thu current      (active, anchor for the card so a nextBlk panel renders)
    - Thu locked       (upcoming, ~+30 days out — drives PB-01 locked window UI)

  Fri Intermediate (class_id=3):
    - Fri old past     (completed, older history)
    - Fri recent past  (completed, just-finished, priority-source)
    - Fri upcoming     (standard window, ~+3 days out)

Customers (3):
  - returning-one@test.example — confirmed on Mon past, Mon current, Wed past
                                  + manual priority on Wed class
  - returning-two@test.example — confirmed on Mon past, Fri recent past, Mon full
  - admin-dummy@test.example   — confirmed on Mon full (fills the cap-2 slot)

Bookings total: 7. Priority grants: 1. (Migration 11 adds NO customers/bookings.)

ROLE-BASED BLOCK LOOKUP
-----------------------
tests/helpers/fixture-lookup.js exports getBlockByRole(role) and
getBlocksByRoles([roles]). Specs MUST use these helpers rather than
hardcoding block IDs — IDs regenerate on every reseed.

Valid roles (11):
  mon-past, mon-current, mon-upcoming, mon-full,
  wed-past, wed-upcoming,
  thu-current, thu-locked,
  fri-old-past, fri-recent-past, fri-upcoming

Lookup strategy: one SELECT query at first call, cached for rest of test run.
Throws FixtureDriftError with "Run npm run seed" guidance on drift.

NPM RUN SEED
------------
`npm run seed` from ~/dev/lg-pilates-booking/tests-playwright reseeds the
test DB by re-applying migration 09.

Behaviour:
- Refuses to run if TEST_SUPABASE_PROJECT_ID or TEST_SUPABASE_DB_URL
  references production
- Prompts for "yes" confirmation (skip with `npm run seed -- --yes` for CI)
- Runs the entire migration in a BEGIN/COMMIT transaction
- Verifies post-run: 11 blocks, 4 classes, 3 customers, 7 bookings, 1 priority

Requires in .env.test:
  - TEST_SUPABASE_DB_URL — direct Postgres connection string for test DB
    (session pooler URI from Supabase dashboard → Project Settings → Database)

TEST DATA FRESHNESS — DRIFT CHECK
----------------------------------
The fixture eventually drifts as "today" moves on. After ~1 week without
reseeding, window states start shifting (priority becomes standard, active
becomes completed, etc.).

Claude runs a drift check at the start of every session (see WORKFLOW
section). If drift is detected, Claude reminds Mark to run:

    cd ~/dev/lg-pilates-booking/tests-playwright
    npm run seed

ALWAYS-ON TEST EVIDENCE
-----------------------
playwright.config.js use block: trace: 'on', screenshot: 'on', video: 'on'

Every test run produces a full trace file, screenshots per step, and a video.

Viewing options:
  npx playwright show-report        # HTML report after any `npm test` run
  npm run test:ui                   # interactive UI runner with step timeline
  npx playwright show-trace <file>  # view a specific trace zip

Disk usage: ~20-50MB per full test run in test-results/. Overwrites each run.

TEST / PROD SCHEMA PARITY
--------------------------
Both projects have a SECURITY DEFINER function get_schema_fingerprint()
that returns a deterministic MD5 hash + object count covering: columns,
constraints, indexes, RLS policies, functions, triggers, and grants in
the public schema. Read-only, granted to anon, exposes nothing sensitive.

scripts/schema-check.js calls the RPC on both projects via their anon keys
and compares the results. Auto-generated constraint and index names are
normalised so cosmetic differences don't trigger a false positive.

To run:
    cd ~/dev/lg-pilates-booking/tests-playwright
    npm run schema-check

Expected output when in sync:
    PROD: dad311c8a152bc0dbec1cb3fa0c584d5  (248 objects)
    TEST: dad311c8a152bc0dbec1cb3fa0c584d5  (248 objects)
    ✅ Schemas are identical.

When to run:
- After applying any schema change to production
- At the start of a session if you suspect drift
- Before a release or deployment milestone
- Quarterly as a routine health check

Gaps the helper does NOT catch:
- Project-level Auth settings (test has its own admin user, prod doesn't)
- Per-project secrets and config (CORS, Resend keys, etc.)
- Data-level differences — fixture data is intentionally different from
  production data; only structure is compared.

Future schema changes to production should also be applied to the test
project, and a `npm run schema-check` run afterwards confirms they landed
in both places.

MAC SETUP
---------
- Node.js v24.15.0 (via nodejs.org LTS)
- npm v11.12.1
- Homebrew 5.1.7
- GitHub CLI (gh) 2.90.0 authenticated as mjones2420-netizen (HTTPS)
- Git 2.50.1 (Apple Git-155)
- Git user.name="Mark Jones", user.email="mjones2420@gmail.com"
- Repo cloned locally at: /Users/markjones/dev/lg-pilates-booking
- Playwright scaffold at: /Users/markjones/dev/lg-pilates-booking/tests-playwright
- .env.test in place (gitignored)
- Local server: python3 -m http.server 8000 (from repo root)
- TEST_APP_URL=http://localhost:8000/?env=test
- devDependencies: @playwright/test, @supabase/supabase-js, dotenv, pg

RUNNING TESTS
-------------
Terminal tab 1 (keep running):
    cd ~/dev/lg-pilates-booking
    python3 -m http.server 8000

Terminal tab 2:
    cd ~/dev/lg-pilates-booking/tests-playwright
    npm test                        # run all tests, generate HTML report
    npx playwright show-report      # open last report in browser
    npm run test:ui                 # interactive UI runner (RECOMMENDED for Mark)
    npm test -- --headed            # show browser during run
    npm test -- --headed --slow-mo 500
    npm run seed                    # reseed test DB (prompts for yes)
    npm run seed -- --yes           # skip prompt (CI)
    npm run schema-check            # verify prod/test schema parity

TEST DATA ACCUMULATION
----------------------
Playwright specs don't clean up after themselves. Each full run adds 3+
test customers (cb01-{timestamp}@test.example etc.). Harmless functionally
as each uses a unique timestamped email, but flag to Mark when Mon block
booked count climbs toward 10.

Cleanest reset: re-run `npm run seed`.

Manual cleanup SQL alternative (test DB only):
  DELETE FROM parq WHERE customer_id IN (SELECT id FROM customers WHERE email LIKE 'cb%-%@test.example');
  DELETE FROM bookings WHERE customer_id IN (SELECT id FROM customers WHERE email LIKE 'cb%-%@test.example');
  DELETE FROM customers WHERE email LIKE 'cb%-%@test.example';
  UPDATE blocks b SET booked = (SELECT COUNT(*) FROM bookings WHERE block_id=b.id AND status != 'cancelled');

============================================================
7. WORKFLOW
============================================================

SESSION START — CLAUDE'S CHECKS
-------------------------------
Run ALL THREE of these EVERY session, regardless of whether the session
looks test-related:

A. Confirm index.html is present
   - Check line count (~2,889 lines post-Session 7)
   - Copy to /home/claude/index.html as the working file

B. Check test DB fixture TIME drift
   - Run this query against project ID ngzfhamjuviwfwuncrjo. It returns
     one row per active/upcoming block with a `drift_warning` flag for
     blocks sitting on the exact boundary of their expected window. The
     coarse bucket-count check used pre-Session-19 was too lenient — it
     passed when blocks were ONE DAY away from slipping out of their
     expected window, which broke a Session 19 Batch 6 test run.

       SELECT
         id, class_id, status, start_date,
         (start_date - CURRENT_DATE) AS days_until_start,
         CASE
           WHEN status = 'active'   AND end_date < CURRENT_DATE + 1                       THEN 'about to complete'
           WHEN status = 'upcoming' AND (start_date - CURRENT_DATE) = 8                   THEN 'about to leave priority window'
           WHEN status = 'upcoming' AND (start_date - CURRENT_DATE) = 15                  THEN 'about to enter priority window'
           WHEN status = 'upcoming' AND (start_date - CURRENT_DATE) = 1                   THEN 'about to become active'
           ELSE NULL
         END AS drift_warning
       FROM blocks
       WHERE status IN ('active','upcoming')
       ORDER BY class_id, start_date;

   - Healthy fixture: every row has drift_warning = NULL. Any non-null
     drift_warning means a block is one day from changing window, so
     reseed before any Playwright work this session.

C. Check test DB fixture STATE drift
   - Detects accumulated junk from previous Playwright runs (stray
     test customers, mon-current creeping toward capacity). Catches
     the failure mode that hit Session 18 (CLASS_FULL on mon-current
     after 13 stray bookings accumulated over multiple test runs).
   - Run this query against project ID ngzfhamjuviwfwuncrjo:

       SELECT
         (SELECT COUNT(*) FROM customers
          WHERE email LIKE 'cb%-%@test.example'
             OR email LIKE 'pb%-%@test.example')                AS stray_test_customers,
         (SELECT COUNT(*) FROM blocks
          WHERE status IN ('active','upcoming')
            AND booked >= cap AND cap > 2)                      AS unexpectedly_full_blocks;

   - Healthy: stray_test_customers low single digits, unexpectedly_full_blocks = 0
   - Note: the seeded mon-full block is intentionally cap=2/booked=2 — the
     `cap > 2` filter excludes it.

   Once Batch 6 (self-cleaning afterEach rollout) is complete, this
   check becomes mostly redundant — specs will clean up after themselves.

   Report combined result for B + C as a single line near the top of the
   opening response:
     ✅ "Test DB fixture looks healthy — all booking windows covered, no state drift."
     ⚠️ "Test DB fixture has drifted — [details]. Run `npm run seed`
          from ~/dev/lg-pilates-booking/tests-playwright before any
          Playwright work this session."

   Don't block the session over either drift check — flag clearly and move on.

DURING SESSION
--------------
- Make all changes to /home/claude/index.html
- Copy final output to /mnt/user-data/outputs/index.html for Mark to download
- If any Playwright test is added or substantially modified, TEST-PLAN.md
  MUST be updated in the same session. Non-negotiable.

AFTER SESSION — MARK'S STEPS
----------------------------
1. Download the output index.html from Claude
2. Replace ~/dev/lg-pilates-booking/index.html with the downloaded file
3. Push to GitHub:

     cd ~/dev/lg-pilates-booking
     git status                    # confirm only index.html is modified
     git add index.html
     git commit -m "TITLE"         # use the title Claude provides
     git push

   Multi-line commit messages with em-dashes or backticks can get tangled
   in zsh quoting. Best to use a SHORT title only.

   Claude's commit-message format: short single-line title.

FILE DRIFT PREVENTION
---------------------
- index.html is uploaded manually at session start — never use a cached version
- If a second index.html is uploaded mid-session, warn Mark and confirm which to use
- Always push the output file to GitHub before ending or starting a session
- Use meaningful commit messages on every push

COMMUNICATION STYLE — MARK'S PREFERENCES
-----------------------------------------
- One action per response, then wait for Mark to confirm before the next
- Don't bury action requests at the end of long explanations
- Keep technical detail brief; plain English summaries only where needed
- Don't offer "I can do this next time" as a buried aside — if it matters,
  track it in context.txt or the TO DO list
- Shorter overall; less narrative; still helpful but focused

============================================================
8. TO DO (priority order)
============================================================

1. PLAYWRIGHT TESTS — continue expanding coverage:

   The full per-tab and per-batch breakdown lives in
   tests-playwright/TEST-PLAN.md. That's the source of truth for what's
   automated, what's outstanding, and which scenarios are duplicates that
   have been red-filled in LG_Pilates_Test_Scenarios.xlsx.

   COMPLETED SUITES:
   - ✅ Client Booking — 33 of 33 Excel scenarios automated (Batches 1-5)
   - ✅ Priority Booking — 10 of 10 Excel scenarios automated (Batches 1-2)
        + 5 PB-X gap-analysis tests (Batch 3, Session 18)
   - ✅ Batch 6 (Sessions 19-20) — self-cleaning afterEach rolled out to 6 specs:
        cb-01 (5 tests inside), cb-03, cb-13, cb-32, pb-09, pb-10.
        Pattern: per-run customers tracked in describe-scope createdCustomerIds[]
        with deleteCustomerCascade in afterEach; fixture customers + per-test
        bookings tracked in createdBooking={customerId,blockId} with
        deleteBookingsForCustomerOnBlock in afterEach.
        cb-02, cb-31, pb-x4 left as-is (cb-02/cb-31 create no state, pb-x4
        is the canonical afterEach example we modelled on).
        CB-33 strengthening completed Session 20 — see Session 20 learnings.
   - ✅ Schedule Display — 6 of 6 Excel scenarios automated (Batch 7, Session 21).
        SD-01 to SD-05 are pure UI filter tests with no DB state. SD-06 hides
        wed-upcoming via `visible=false` with a self-cleaning afterEach that
        restores the original value regardless of pass/fail.
   - ✅ Admin Clients — 2 of 2 genuine scenarios automated (Batch 8, Session 22).
        ACL-01 asserts the customer table renders with the live 6-column schema
        and all seeded fixture clients appear. ACL-02 asserts the three-state
        overall priority badge (Manual / Auto / Standard) renders correctly per
        client state, with a self-cleaning per-run Standard-state customer.
        Excel wording updated in same session — old text referenced the
        deprecated global customers.priority column and a two-state badge model;
        live UI uses per-class priority via customer_class_priority and three
        badge states with manual > auto > standard precedence.
   - ✅ Booking Windows — 3 of 3 genuine scenarios automated (Batch 9, Session 22).
        BW-01 asserts a single-block class card has no next-block panel.
        BW-02 asserts the active block's date pills match the dates[] array in
        chronological order. BW-06 asserts a block with start_date <= today
        renders as current (via getActiveBlock's date logic, not status column).
        All read-only — no DB writes. fixture-lookup.js extended to include
        `dates` column in its SELECT for BW-02 (pure addition, no existing
        spec read that field).
   - ✅ Security — 3 of 3 genuine scenarios automated (Batch 10, Session 23).
        SEC-02 (2 tests) confirms anon SELECT on settings table directly AND
        the functional consequence — bank details render on the payment screen.
        SEC-06 (3 tests) confirms admin login works, all 4 dashboard tabs
        render their panels, and the 3 below-tab sections (Upcoming Classes,
        Settings, Backup & Export) render their headings + primary controls.
        SEC-07 (1 test) audits the live anon grant matrix via direct pg
        against information_schema.role_table_grants — fails loudly if anon
        gains unexpected grants OR loses expected ones. Excel updated in same
        session: SEC-03 red-filled as duplicate of CB-01; SEC-06 wording
        corrected from "every tab loads (incl. Settings, Backup & Export)"
        to "4 tabs + 3 below-tab sections".

   DUPLICATE SENSE-CHECK (10 May 2026, updated 16 May 2026):
   11 Excel rows red-filled as no longer requiring automation — see
   TEST-PLAN.md Coverage Tracker for which scenarios are covered by which
   existing tests. Affected: BW-03, BW-04, BW-05, BW-07, EC-02, SEC-01,
   SEC-03, SEC-04, SEC-05, ACL-03, ACL-04. Block Warnings tab IDs renamed
   BW-* → BLW-* to disambiguate from Booking Windows tab.

   UPCOMING BATCHES (priority order, full detail in TEST-PLAN.md):
   - Batch 11 — Edge Cases (part 1)
   - Batch 12 — Edge Cases (part 2)
   - Batch 13 — Block Warnings (BLW)
   - Batch 14 — Settings & Export
   - Batch 15 — Admin Bookings (part 1)
   - Batch 16 — Admin Bookings (part 2)
   - Batch 17 — Admin Bookings (part 3)
   - Batch 18 — Admin Classes (part 1)
   - Batch 19 — Admin Classes (part 2)
   - Batch 20 — Admin Classes (part 3)

   NEXT SESSION FOCUS — start Batch 11 (Edge Cases part 1):
   - BATCH 11 — Edge Cases (first 6 EC scenarios). Scope per TEST-PLAN.md.

   INFRASTRUCTURE BACKLOG (post-batches):
   - GitHub Actions CI for auto-run on push
   - Mobile Safari Playwright project for proper mobile coverage
     (currently CB-29/CB-30 use shrunken desktop viewport at 480x700 as
     a proxy — works but doesn't catch real mobile-specific issues)

   REMINDER: TEST-PLAN.md must be updated in the same session as any new
   test, BUT only after tests are verified passing (Session 16 learning).

2. INDEX.HTML — minor cosmetic bug:
   Line 476 still reads "Step 1 of 3 — Your Details" — leftover from the
   old 3-step flow. Lines 501, 558, 576 correctly say "Step 2 of 4",
   "Step 3 of 4", "Step 4 of 4". The pip indicator itself is correct;
   only this in-app step-label text needs fixing.

3. EMAIL NOTIFICATIONS via Resend
4. CLASS REGISTER FEATURE
5. DEMO FILE UPDATES (multiple outdated since per-class priority + pro-rata + 4-step flow)
6. HONEYPOT anti-bot protection
7. BANK DETAILS — replace placeholder with Louise's real details
8. HOSTING MIGRATION & CUSTOM DOMAIN (GitHub Pages → Netlify, book.lg-pilates.co.uk)
9. USER GUIDE — update PDF at post-email milestone
10. WAITLIST FEATURE — table exists, not yet connected
11. FILE SPLIT — consider at next major milestone
12. AUDIT ITEMS:
    - Swap Supabase anon key to newer publishable key format (at Netlify migration)
    - Enable leaked password protection in Supabase Auth dashboard (manual toggle)

============================================================
9. KEY DECISIONS & GOTCHAS
============================================================

These are durable, hard-won lessons. Add new ones sparingly — only if
they'd save a future session real time.

DATABASE & SUPABASE
-------------------
- customer_type values must be lowercase: 'new', 'returning', 'vip' (or NULL).
- blocks.dates[] stores display-formatted strings ("6 Apr", "13 Apr"), NOT ISO dates.
- Front-end JS must never rely on direct SELECT against customers or bookings
  from an anon context — RLS silently returns empty arrays rather than erroring.
- Three-layer defence against duplicate bookings (DB unique index, RPC catch,
  front-end early detection).
- PAR-Q presence is flagged in the dashboard for new-client bookings only.
- Supabase web SQL editor has a parser bug with PL/pgSQL DECLARE vars.
  Use apply_migration (MCP) or Supabase CLI for any DDL involving function bodies.
- supabase_migrations.schema_migrations stores full SQL of applied migrations
  in its `statements` column — useful for reconstructing migrations applied
  via MCP without a local file.
- Test admin users on Supabase MUST be created via the dashboard, not via raw
  SQL. Raw inserts into auth.users don't populate auth.identities, which
  Supabase Auth requires for password login.
- After raw SQL changes to bookings, manually resync blocks.booked — the
  trigger only fires on app-level inserts/deletes (see DATABASE TRIGGERS).
- The lg-pilates-test Supabase project is a byte-for-byte mirror of production
  schema. Any future production schema changes MUST be mirrored to test or
  tests will drift from reality.

PLAYWRIGHT / TEST INFRA
-----------------------
- Excel test scenarios live in LG_Pilates_Test_Scenarios.xlsx. As of
  Session 22 (15 May 2026) this file is committed to git at the repo root
  alongside index.html and context.txt — pushed and updated whenever
  scenarios change. Three gotchas: (a) the "Block Warnings" tab IDs were
  renamed BW-* → BLW-* on 10 May 2026 to disambiguate from the "Booking
  Windows" tab. So BLW-01 to BLW-08 are the dashboard banner tests; BW-01
  to BW-06 are the booking-window UI states. (b) Red-filled rows in the
  Excel file are duplicates of existing automated tests or housekeeping
  steps — do NOT pick these up in future batches. The TEST-PLAN.md
  Coverage Tracker is the source of truth for which Excel scenario maps
  to which automated spec. (c) Excel styling pattern: alternating row
  fills FFFFFFFF / FFEEF5F5 (white / cream), SQL column (column 5) uses
  Courier New 8pt with FFE8F0FE fill (pale blue). Preserve these when
  updating rows via openpyxl.
- tests/helpers/supabase.js exports `sb`, NOT `supabase`. Any new helper
  importing from it must use `const { sb } = require('./supabase')`.
- Spec helper imports use './helpers/...' (same level), NOT '../helpers/...'.
  Helpers live INSIDE tests/, not above it.
- Block IDs are NOT stable across reseeds — specs must look up by role
  via fixture-lookup.js, never by literal ID.
- Playwright's baseURL strips query strings from page.goto('/'). Always
  use the APP_PATH helper from tests/helpers/app-url.js.
- Every CB/AB/PB spec's beforeEach must assert #test-mode-banner.on before
  doing anything else — fail fast, fail loud. The browser-page Supabase
  client is NOT covered by the safety-check.js helper guard.
- openBookingModal helper signature is (page, day, which) — `day` is a
  string like "Monday", NOT a numeric class_id. `which` is 'current'
  (default) or 'next'.
- Button labels on class cards depend on block state:
    "Book Current Block"  — active blocks AND upcoming when no active block
    "Book Next Block"     — only in standard window (0-7 days out)
    "Check My Priority"   — priority window (8-14 days out), no direct book
  When in doubt, use 'current' as the openBookingModal arg.
- Step pip mechanism: completed steps swap textContent from the number to
  a tick (\u2713). Assertions need toHaveText('\u2713'), not just a class
  check. The .done class is added but the visual change comes from BOTH.
- Step pip back-nav for returning clients: goStepBack3() returns to step 1
  directly (no intermediate step in 2-step layout). The "Payment" label on
  pip 2 persists across back/forward — only resets on full modal close + reopen.
- Modal scroll-to-top uses scrollTo({behavior:'smooth'}). Playwright
  assertions need expect.poll on scrollTop with a short timeout (~2s).
  Hard-asserting toBe(0) immediately will race the smooth scroll.
- CB-13 uses test.skip() (not expect().toBe(false)) on its pre-flight
  already-booked check, so a successful first run leaves the suite green
  on the second run.
- CB-21 and CB-28 already test parts of step pip behaviour. Batch 3 specs
  (CB-14, CB-15, CB-16b) overlap. Decision: keep both. Atomic Batch 3
  specs make failures clearer; slight redundancy is the price for diagnostic clarity.
- Priority gate UI is rendered ONLY on a class card's nextBlk panel — a
  class needs both a current block AND an upcoming block for the gate to
  appear. In Migration 09 only Monday meets this (mon-current active +
  mon-upcoming). Wed and Fri have one upcoming block each which renders as
  "current" via getActiveBlock fallback — no nextBlk panel, no gate. Always
  verify the rendering path actually fires on the chosen card BEFORE writing
  a spec — Session 16 burned a debug cycle on this when PB-02/PB-04 were
  pointed at Wednesday based on data-layer eligibility alone.
- check_priority_access RPC selects "previous block" as the most-recent
  end_date < the upcoming block's start_date — this can be an active block
  (mon-current is the previous block for mon-upcoming, NOT mon-past). When
  setting up priority specs, always run `check_priority_access` directly
  for the planned customer/block pair rather than reasoning about it from
  the bookings table alone.
- book_if_available RPC always inserts as status='reserved' and bypasses
  block status checks (only enforces capacity + duplicate detection). Useful
  for spec setup when you need a reserved booking on any block — including
  active blocks — without involving the admin login flow.
- Migration 11 adds the Thursday class for PB-01's locked-window UI. seed.js
  was extended to apply 09 + 11 in sequence inside one transaction. Any
  future fixture-affecting migrations must (a) be added to MIGRATION_FILES
  in seed.js and (b) update the EXPECTED counts. Smoke-01 also asserts
  exact totals — check that too on any fixture change.
- File-on-disk vs DB-state divergence is a real failure mode: Session 16
  applied a Tuesday→Thursday change via Supabase MCP but didn't update the
  saved migration file. Tests passed until the next reseed re-applied the
  stale file. Rule: when a change has both a "live apply" and a "file save"
  component, do them from the same source string.
- The shared `sb` client in tests/helpers/supabase.js is anon-only. It has
  NO grants on customer_class_priority, bookings, customers, cancellations,
  or waitlist. Calls against those tables return null/empty silently rather
  than erroring (RLS behaviour). For fixture writes/reads on admin-only
  tables, use tests/helpers/admin-db.js (Session 17) which opens a direct
  Postgres connection via TEST_SUPABASE_DB_URL and bypasses RLS. Pattern:
  `const { grantManualPriority, hasManualPriority } = require('./helpers/admin-db')`
  Same pg pool also useful for any future AB or stronger CB-33 tests.
- Admin login helper: tests/helpers/admin-auth.js (Session 17) drives the
  dashboard login UI via #dash-email / #dash-password. Requires
  TEST_ADMIN_EMAIL and TEST_ADMIN_PASSWORD in .env.test. Session is held
  in the browser localStorage, not in the test process. signOutAdmin() is
  also exported. Per-class panel utilities included:
  expandPerClassPanel, classPriorityButton(cust,cls,'grant'|'remove'),
  classPriorityBadge(cust,cls).
- Per-class panel button selectors use the onclick attribute pattern:
  `button[onclick="toggleClassPriority(custId,classId,true|false)"]`.
  Third arg is true=Remove (currently granted), false=Grant (not granted).
  Stable across UI restructure since onclick wiring is in the rendered HTML.
- When admin signs out via signOut(), show("schedule") only UN-HIDES the
  existing schedule DOM — it does NOT re-render. Any panels open before the
  admin session (e.g. next-blk-body toggled open in a pre-admin step) remain
  open after sign-out, with their input values intact. Re-clicking a toggle
  in this state will CLOSE it. Either avoid the second toggle click, or
  expect the panel to already be open. PB-08 hit this in Session 17.
- Admin login does not work without auth.identities populated. Test admin
  users MUST be created via the Supabase dashboard "Add user" button —
  raw INSERTs into auth.users skip the identity row and password sign-in
  fails silently. (Already documented above under DATABASE & SUPABASE.)
- Per-class priority gate UI rendering rule (Session 16, re-confirmed
  Session 17): the gate only renders on a class card that has BOTH an
  active current block AND an upcoming block. Wednesday (no active in
  current fixture) and Friday (no priority-window upcoming) do not render
  the gate. Monday is the only fixture class with a working gate. Verify
  via `EXISTS(blocks WHERE status='active' AND today between start/end)`
  AND `EXISTS(blocks WHERE status='upcoming' AND start_date - today
  BETWEEN 8 AND 14)` BEFORE planning a gate-driven spec.

CB-12 NOTE
----------
CB-12 has no standalone spec file. It's covered by CB-01's spec (the
new-client happy path naturally exercises T&Cs agreement). Coverage
Tracker shows it as ✅ with that note.

PB-03 NOTE
----------
PB-03 has no standalone spec file. It's covered by PB-10 (confirmed
booking on previous block grants priority — same assertion as PB-03's
eligible-client scenario). Coverage Tracker shows it as ✅ with that note.

PB-08 NOTE
----------
PB-08's Excel scenario originally referenced the Wednesday class. The
current fixture's Wednesday card has no active block — only an upcoming
one — which means the priority gate UI does NOT render on it (the gate
only appears when a class has both an active current block AND an
upcoming block). Monday is the only fixture class where the gate
renders, so PB-08 was implemented against Monday using returning-two
(confirmed on mon-past and mon-full but NOT on mon-current, so denied
without a manual grant). Excel scenario wording was updated in Session
17 to match.

WORKFLOW DISCIPLINE — SESSION 16 LEARNING
-----------------------------------------
Recurring failure mode: Claude making unilateral mid-batch changes
(deferring specs, updating documentation early, adding "while I'm at it"
fixes) instead of stopping to ask.

Hard rule going forward: any time the agreed batch scope changes for ANY
reason — spec deferral, fixture change, obsolete scenario, selector
mismatch, customer/block pair unworkable — STOP. Surface in two lines.
Wait for explicit sign-off before acting.

Documentation is locked until tests pass: do NOT update TEST-PLAN.md,
context.txt, or the Excel scenarios sheet during a batch in progress.
The trigger for updating them is the user explicitly saying tests are
green or asking for the update.

Self-trigger phrases — if Claude finds itself about to type any of these,
that's the cue to stop and ask: "I'll just...", "Easiest thing is to...",
"Let me also...", "While I'm at it...", "It makes sense to...",
"I'll defer/skip/replace this for now..."

This is captured formally in the playwright-batch-workflow skill (STEP 2.5).

SESSION 18 LEARNINGS — SELECTORS, ASYNC RACES, FIXTURE STATE DRIFT
-------------------------------------------------------------------
- Class card root selector is `.card`, NOT `.class-card`. Several Session 18
  specs failed initially because they used `.class-card`. When in doubt,
  click the toggle by its unique onclick attribute instead of finding the
  card first: `[onclick="toggleNextBlk('nb-${blockId}')"]` — that selector
  is stable and tied to the specific block.
- The admin login helper expects `#tab-bookings.on` after login. After
  `openClientsTab` is called, the tab classes are sticky and don't reset on
  signOut. Re-login in the same test fails because tab-bookings is no
  longer the active tab. Workaround: `page.goto(APP_PATH)` (full reload)
  before the second loginAsAdmin call. The session was already cleared by
  signOut, so the reload lands on the login form as expected.
- toggleClassPriority is a fire-and-forget IIFE: clicking Grant/Remove
  awaits a DB write, shows a toast, then calls renderCustomersTab() which
  itself awaits 4 DB reads before swapping tbody innerHTML. Clicking the
  Per-class button to re-expand the panel BEFORE this re-render completes
  hits a stale (now-detached) row. Fix: after Grant/Remove click, wait for
  `#toastEl.on` to be visible AND for the next button (Remove if you just
  granted, Grant if you just removed) to exist via `toHaveCount(1)`. Then
  re-expand the panel. PB-X5 has the canonical pattern.
- Fixture state drift across multiple test runs is a real failure mode.
  Specs that book the same fixture customer on a fixture block (CB-01,
  CB-07, CB-13, CB-31, CB-32, CB-33, PB-09, PB-10, PB-X4) accumulate
  bookings on mon-current and other shared blocks across runs. Session 18
  hit a CLASS_FULL failure when mon-current reached cap=12 with 13 stray
  bookings from earlier runs. The trg_sync_block_booked_count trigger
  keeps blocks.booked accurate, but the UNDERLYING DATA is the problem,
  not the count. The fix applied (Session 18) was a one-time manual
  cleanup of mon-current + a self-cleaning afterEach in PB-X4. The full
  fix (Option B in TO DO) is to add afterEach cleanup to every spec that
  creates state. Until that lands, expect to occasionally need a manual
  cleanup or full `npm run seed` after heavy iteration.
- bookings → customers FK is NOT ON DELETE CASCADE. parq → bookings IS
  ON DELETE CASCADE. So to delete a customer, delete their bookings
  first (parq cascades), then delete the customer. context.txt's stated
  deletion order is the canonical reference: parq → cancellations →
  bookings → customer_class_priority → customers. (Cancellations are
  denormalised audit records — they don't get deleted by routine cleanup.)
- The shared `sb` anon client returns null/empty silently when calling
  RPCs that fail server-side (e.g. CLASS_FULL). Always check the `error`
  return, not just `data`. Pattern: `const { data, error } = await sb.rpc(...)`
  then `expect(error).toBeNull()` BEFORE asserting on `data`.
- deleteBookingsForCustomerOnBlock only deletes status != 'cancelled'.
  PB-X4 leaves a cancelled booking by design — that booking is junk
  state that won't be removed by entry-side self-cleaning, which is why
  PB-X4 needed an afterEach with deleteCustomerCascade instead. Specs
  that flip booking status to cancelled MUST clean up via customer
  cascade, not booking-level delete.

SESSION 19 LEARNINGS — FIXTURE DRIFT EDGE CASES, BATCH 6 ROLLOUT
----------------------------------------------------------------
- The session-start fixture drift check was too coarse pre-Session 19.
  It used bucket counts (priority_window >= 1, etc.) which passed even
  when a block was ONE DAY from slipping out of its expected window.
  Session 19 hit this: mon-upcoming was at exactly 8 days at session
  start (priority_window=1, looked healthy), but ticked over to 7 days
  during the test run, breaking every PB spec that needed the priority-
  window UI. The check has been replaced with a per-block boundary-
  aware query — see Section 7 Check B. Anything with a non-null
  drift_warning needs a reseed before Playwright work.
- Batch 6 (self-cleaning afterEach) rolled out to 6 specs:
    cb-01 — 5 tests inside, each pushes its customer id into a
            describe-scope createdCustomerIds[] array; afterEach loops
            and calls deleteCustomerCascade on each.
    cb-03, cb-13, cb-32, pb-10 — fixture customer + per-run booking;
            track createdBooking={customerId,blockId}; afterEach calls
            deleteBookingsForCustomerOnBlock.
    pb-09 — per-run customer; track createdCustomerId; afterEach calls
            deleteCustomerCascade.
  Specs NOT touched: cb-02 (no Reserve click, no state created),
  cb-31 (verifies already-booked screen without creating new bookings),
  pb-x4 (already canonical example we modelled on).
- Pattern: set the tracking variable BEFORE the UI flow runs so afterEach
  cleans up even if UI assertions fail. Reset in beforeEach.
- CB-33 STRENGTHENING WAS ATTEMPTED in Session 19 and PARKED — resolved
  in Session 20 (see below). Original symptom: parq INSERT returning 401
  from PostgREST (error 42501, permission denied). The fix was a one-line
  front-end change, not a grant change. See Session 20 learnings for the
  full diagnosis.

SESSION 20 LEARNINGS — PARQ 401 ROOT CAUSE, .SELECT() CHAINING PITFALL
---------------------------------------------------------------------
- Root cause of the CB-33 parq 401: the front-end's parq insert was
  `await sb.from("parq").insert({...}).select()`. The trailing `.select()`
  makes PostgREST add `RETURNING *`, which requires SELECT permission.
  Anon has INSERT but not SELECT on parq, so PostgREST returned 42501.
  The booking flow's try/catch swallowed the error and showed the
  success view — so the bug was invisible in production. The CB-33 test
  was the only thing that surfaced it.
- The lingering production mystery (Del Trotter, Clark Bob parq rows
  existing post-Item-20 migration despite SQL-layer rollback in tests)
  was NOT fully chased. At SQL level, INSERT...RETURNING with permission
  denied DOES roll back the row (verified directly). At PostgREST HTTP
  level, the row must NOT roll back — that's the only way prod parq
  rows could exist. The behaviour difference between SQL and PostgREST
  on RETURNING failures is real but not yet investigated. Not worth
  chasing further since the .select() removal eliminates the issue
  entirely.
- The fix (one line in index.html, line ~1554): drop the `.select()` from
  the parq insert. The returned value was never consumed. Same data
  goes in, no RETURNING is requested, no 401 fires. Production now
  also runs without the silent console error.
- CB-33 strengthening (deferred from Session 19) landed cleanly: imports
  `getParqByCustomerId` from admin-db.js, asserts parq row exists with
  matching customer_id, booking_id, print_name, sign_date, and that
  q1_heart and q12_other_reasons are 'No'. All 7 tests in
  cb-01-new-client-happy-path.spec.js pass (33.9s wall clock).
- Pattern to watch for: any `.insert(...).select()` chain on a table
  where anon has INSERT but not SELECT will silently fail in production
  with the same symptom. Search for similar chains before adding new
  anon-write paths. Currently only parq is affected.

SESSION 21 LEARNINGS — VISIBLE vs STATUS, DOCUMENTATION DISCIPLINE
------------------------------------------------------------------
- Front-end block-hiding lever is `blocks.visible`, NOT `blocks.status`.
  getActiveBlock() at line ~886 of index.html filters with
  `b.visible !== false` and does NOT check status at all. So flipping
  status from 'upcoming' to anything else has zero effect on what the
  public grid shows. Confirmed twice over in Session 21: first via a
  failed UPDATE to status='archived' (rejected by blocks_status_check;
  not a valid status anyway), then via UPDATE to status='cancelled'
  which the constraint allowed but the page still showed the class.
  Reading the actual filter function on attempt 3 surfaced the right
  column. Rule: before assuming what column the front-end filters on,
  read the actual filter function in index.html — the Excel SQL hints
  (e.g. SD-06's "UPDATE blocks SET status = 'archived'") are not always
  accurate.
- blocks_status_check allows ONLY: 'upcoming', 'active', 'completed',
  'cancelled'. 'archived' is NOT a valid status. The Excel scenario for
  SD-06 used 'archived' in its example SQL — that wording is wrong and
  was left as-is in the Excel for now (not a real blocker since the
  spec mechanism is documented in code and TEST-PLAN.md).
- Documentation discipline reinforced: I jumped the gun on the
  TEST-PLAN.md update before the SD-06 spec was green, then had to fix
  the mechanism description twice after the spec was finally working.
  Rule (already documented under WORKFLOW DISCIPLINE — SESSION 16
  LEARNING): do NOT update TEST-PLAN.md, context.txt, or the Excel
  scenarios during a batch in progress. The trigger is the user
  explicitly saying tests are green. Re-stating it here because the
  failure mode keeps recurring.
- The Excel SD-06 wording references "Run the SQL to remove a block's
  status" with example SQL setting status='archived'. This is wrong on
  two counts (invalid status value AND wrong column). If the Excel
  scenarios are ever revised, SD-06 should be updated to reference
  `visible = false` as the actual mechanism.

SESSION 22 LEARNINGS — STALE EXCEL WORDING, STRICT-MODE LOCATORS, HELPER COVERAGE
--------------------------------------------------------------------------------
- The Admin Clients (ACL) and Booking Windows (BW) Excel scenarios were
  written against the original schema/UI and had drifted from reality.
  ACL-01/02 referenced the deprecated global `customers.priority` column
  and a two-state badge model — the live UI uses per-class priority via
  `customer_class_priority` and renders three badge states (Manual / Auto
  / Standard) with manual > auto > standard precedence. Excel scenarios
  updated in the same session, with the spec testing the live behaviour.
  Lesson: when picking up a long-deferred Excel tab, expect to find stale
  wording — check what the live system actually does BEFORE locking
  scope, not after writing the spec.
- Playwright strict mode bites when a locator matches multiple elements.
  Pattern that fails: `tbody.locator('tr').not.toHaveText(/Loading.../)`
  on a multi-row tbody. The fix is to wait for a specific known row to
  appear via `tbody.locator('tr', { hasText: 'known-email' }).first()`
  — both proves the render finished AND avoids the multi-match.
- The card.locator('.block-dates-pills .date-pill') pattern grabs pills
  from BOTH the current-block section AND the next-block panel (line
  1041 of index.html puts a second .block-dates-pills inside .next-blk-body).
  When asserting pill counts/order on a card with a next-block panel,
  scope with `.first()`: `card.locator('.block-dates-pills').first()
  .locator('.date-pill')`. The current block always renders first.
- fixture-lookup.js had been written before any spec needed the blocks.dates
  column. When BW-02 needed it, the field was absent from the SELECT and
  silently undefined. Fix was to add `dates` to the SELECT list — pure
  addition, no existing spec read that field, zero blast radius. Rule:
  when a shared helper omits a column a new spec needs, prefer extending
  the helper to a one-off direct query — the helper change is more
  durable AND lower risk than it looks.
- The default fixture-lookup helper SELECT now returns: id, class_id,
  status, start_date, end_date, cap, booked, dates. Any future spec
  needing other block columns (price, weeks, visible) should extend the
  SELECT the same way.
- BW-06 sanity check: Monday is the ONLY fixture class with both an
  active block AND a separate upcoming block. Any spec wanting to test
  the active-vs-next distinction must target Monday — Wed/Thu/Fri don't
  exhibit both states simultaneously.
- The mockup-first-ui skill applies to UI/feature changes, not to test
  writing. Don't waste cycles producing a mockup before adding a spec —
  that's not the skill's intended trigger.

SESSION 23 LEARNINGS — BOOKING FLOW WRITE BOUNDARIES, GRANT MATRIX AUDIT, DASHBOARD STRUCTURE
---------------------------------------------------------------------------------------------
- Customer is created ONLY at the Reserve click in Step 4, not earlier.
  goStep2() (the Step 1 → Step 2 advance) calls lookup_customer to detect
  returning clients, but does NOT call upsert_customer. The upsert + the
  book_if_available RPC both run inside the Reserve click handler (line
  ~1505 in index.html). Implication for specs: a spec that stops short of
  Reserve creates NO customer/booking row, even if it filled Step 1 and
  advanced through Step 2a/2b. No afterEach cleanup needed in that case
  (SEC-02 second test is the canonical example). Conversely, a spec that
  clicks Reserve always creates a customer AND a booking, so cleanup must
  track both.
- Direct pg via admin-db.js is the right tool for grant matrix audits.
  Anon SELECT on information_schema returns role-relative results (anon
  only sees its OWN grants), which is useless for a "what does anon have"
  audit. The TEST_SUPABASE_DB_URL pool sees everything. SEC-07's query
  groups by table_name and aggregates privilege_type — the canonical
  pattern. The same approach works for grant audits on any role.
- The admin dashboard has 4 tabs + 3 below-tab sections, NOT 6 tabs:
    Tabs:     #tab-bookings, #tab-classes, #tab-customers, #tab-cancellations
    Sections: Upcoming Classes (#ctbody), Settings (#setting-bank-*),
              Backup & Export (Export buttons)
  Sections render unconditionally below the tab strip — they're always
  visible once logged in. Excel scenarios written before this distinction
  was clear (notably the old SEC-06 wording) referred to all 6 as "tabs".
  When picking up Admin Settings / Admin Classes batches, target sections
  by their headings/inputs/buttons rather than expecting a tab-style
  click-through.

============================================================
