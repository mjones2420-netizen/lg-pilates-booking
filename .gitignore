.DS_Store
supabase/.temp/
.claude/commands/

# internal session notes — not for public repo
context.txt
context_old.txt

# review-before-tests gate marker (transient, local)
.claude/.review-marker

# tool-generated dev-server launch config, local only. Wraps the same
# python3 -m http.server 8000 command already documented in CLAUDE.md.
.claude/launch.json
